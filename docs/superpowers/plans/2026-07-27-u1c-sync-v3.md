# U1c — Sync mang được habit v3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Đẩy `value` / `completedAt` của mỗi ô log và toàn bộ field định nghĩa habit v3 qua sync, để hai thiết bị thấy cùng một thói quen v3 chứ không phải một bóng mờ v2 của nó.

**Architecture:** Mở rộng **thuần cộng thêm** (purely additive). `habit_logs.done` vẫn là boolean nguồn sự thật — client tính bằng `isEntryComplete` (chỉ client biết luật tracking), còn `value`/`completed_at` là chi tiết đi kèm. Nhờ vậy `refresh_my_summary` (Garden Fair) và vòng lặp `shared_rhythms` đọc `done` không phải sửa một dòng nào. Cột mới đều `add column if not exists`, RPC mới đều `drop` chữ ký cũ trước khi `create or replace`, và cả hai chiều client↔server đều chịu được đối phương còn ở schema cũ.

**Tech Stack:** PostgreSQL (Supabase) + PostgREST RPC, TypeScript 5.9 strict, Next.js server actions, Vitest.

## Global Constraints

- **`done` không đổi nghĩa.** `habit_logs.done` vẫn là boolean "hôm đó xong chưa", client tính, server không suy diễn. Ba nơi đang đọc nó (`get_sync_snapshot`, `refresh_my_summary` §5.2, vòng `shared_rhythms` §5.1) phải chạy nguyên vẹn.
- **Mọi thay đổi SQL phải idempotent** — `schema.sql` chạy lại nhiều lần vẫn đúng (`add column if not exists`, `create or replace`, `drop ... if exists`).
- **Chữ ký hàm đổi ⇒ phải `drop function if exists` chữ ký CŨ trước.** `create or replace function` với danh sách tham số khác sẽ tạo **overload**, không thay thế. Hai overload cùng tên + gọi bằng named argument (PostgREST luôn gọi thế) = lỗi `42725 function is not unique`.
- **Grant phải nêu đúng chữ ký mới.** Hàm Postgres mặc định `EXECUTE` cho `PUBLIC`; drop hàm cũ là mất luôn grant cũ, nên hàm mới bắt buộc có cặp `revoke ... from public, anon` + `grant ... to authenticated`.
- **Client mới + schema cũ phải tự lành.** Gọi RPC thiếu tham số → PostgREST trả `PGRST202`; lỗi này phải xếp loại **retry**, không phải permanent — để hàng đợi tự đẩy lại ngay khi chủ repo apply SQL.
- **Server cũ + client mới phải không vỡ.** Snapshot không có `value` ⇒ merge giữ nguyên hành vi hiện tại (tick từ xa giữ lại giá trị giàu hơn ở máy này).
- **Không đụng `seedCutoverDate`, `bestStreakFloor`, `events`** — chúng không có mutation kind và không bao giờ rời máy (spec §2.5).
- Tiếng Việt trong mọi chuỗi hiển thị; comment code bằng tiếng Anh, giải thích *tại sao*.

---

## File Structure

| File | Trách nhiệm | Việc của U1c |
|---|---|---|
| `supabase/schema.sql` | Toàn bộ schema + RPC | Cột mới trên `habits`/`habit_logs`; nới `apply_habit_log`, `upsert_habit`, `get_sync_snapshot`; grant lại |
| `tests/schema-contract.test.ts` | **Tạo mới** — gate cho SQL khi CI không có DB | Bắt lỗi overload và grant lệch chữ ký |
| `src/lib/sync/types.ts` | Hợp đồng dữ liệu sync | `SetHabitLogMutation` + `value`/`completedAt`; `SyncHabitPayload`/`ServerHabit` + field v3; `ServerHabitLog` + `value`/`completedAt` |
| `src/lib/sync/queue.ts` | Gộp mutation trùng | Không đổi logic — chỉ cần test chứng minh payload giàu hơn được giữ |
| `src/lib/sync/merge.ts` | Toàn bộ quyết định hợp nhất | Ô log mang `value`/`completedAt`; habit mang field v3 hai chiều |
| `src/lib/server/sync-actions.ts` | Cầu nối RPC | Truyền tham số mới; parse cột mới; PGRST202 = retry |
| `src/lib/server/sync-actions.test.ts` | **Tạo mới** | Chứng minh đúng tham số được gửi và cột mới được parse |
| `src/lib/sync/importer.ts` | Upload lần đầu | Gửi `value`/`completedAt` thật + field v3 |
| `src/components/app/state-provider.tsx` | Nơi phát sinh mutation | Gửi `value`/`completedAt`; **vá lỗ tiến độ dở dang không bao giờ được đẩy lên** |
| `docs/superpowers/specs/2026-07-08-social-garden-spec.md` | Spec sync | Thêm "Amendment 2026-07-27" ở cuối |
| `AGENTS.md`, `HANDOFF.md` | Tài liệu cho người sau | Quy ước mới + trạng thái |

---

## Bối cảnh: ba chỗ dễ sai mà người thực thi phải biết trước

**(1) Lỗ tiến độ dở dang.** `state-provider.tsx` hiện chỉ enqueue khi *số habit đã xong trong ngày* đổi:

```ts
if (after !== before) {
  markSyncDirty({ kind: "setHabitLog", ... });
}
```

Uống từ 3 → 4 ly (mục tiêu 8) không đổi `after`, nên **không có mutation nào được tạo**. Hôm nay điều đó đúng (boolean chưa đổi). Từ U1c trở đi nó là lỗi mất dữ liệu: tiến độ dở dang vĩnh viễn không rời máy. Task 6 vá chỗ này.

**(2) Overload hàm.** Xem Global Constraints. Đây là lỗi chỉ nổ trên DB thật, không test nào ở máy bắt được — nên Task 1 có test đọc thẳng file `.sql`.

**(3) `completedAt` là "HH:mm" giờ địa phương, không phải timestamp.** `LogEntry.completedAt` cố tình chỉ có giờ-phút vì ngày đã nằm ở khoá record; giữ nguyên như vậy khi qua dây để không dính lệch múi giờ. Cột DB là `text`, không phải `timestamptz`.

---

### Task 1: Schema — cột mới, RPC nới, và một gate đọc file SQL

**Files:**
- Modify: `supabase/schema.sql` (bảng ~40-52, RPC ~378-490, snapshot ~776-808, grants ~812-830)
- Create: `tests/schema-contract.test.ts`

**Interfaces:**
- Consumes: không có.
- Produces: RPC `apply_habit_log(text, date, boolean, timestamptz, integer, text)`; RPC `upsert_habit(text, text, text, numeric, boolean, text, integer, timestamptz, boolean, text, text, numeric, text, jsonb, jsonb, jsonb, text, text, text, text, text)`; `get_sync_snapshot()` trả thêm `value`, `completedAt` trong `logs[]` và 12 field v3 trong `habits[]`.

- [ ] **Step 1: Viết test đọc file SQL (gate cho thứ CI không chạy được)**

Tạo `tests/schema-contract.test.ts`:

```ts
import { readFileSync } from "node:fs";
import { describe, expect, it } from "vitest";

/**
 * CI has no database, so nothing here executes SQL — this reads schema.sql as
 * text and guards the two mistakes that only ever show up on the real
 * instance: a widened function silently becoming an OVERLOAD of its old self
 * (PostgREST then fails every named-argument call with 42725), and a grant
 * still naming the old signature (so the new function keeps Postgres' default
 * EXECUTE-to-PUBLIC and is reachable by `anon`).
 */

const SCHEMA = readFileSync(new URL("../supabase/schema.sql", import.meta.url), "utf8");

/** Every `create or replace function public.NAME(...)` with its parameter types. */
function definedFunctions(): Map<string, string[]> {
  const out = new Map<string, string[]>();
  const re = /create or replace function public\.(\w+)\s*\(([\s\S]*?)\)\s*returns/g;

  for (const match of SCHEMA.matchAll(re)) {
    out.set(match[1], parameterTypes(match[2]));
  }

  return out;
}

/** "p_key text, p_ts timestamptz default now()" -> ["text", "timestamptz"] */
function parameterTypes(params: string): string[] {
  return params
    .split(",")
    .map((part) => part.trim())
    .filter((part) => part.length > 0)
    .map((part) => part.split(/\s+/)[1]);
}

describe("schema.sql — habit sync columns", () => {
  it("adds the v3 log detail columns idempotently", () => {
    expect(SCHEMA).toMatch(
      /alter table public\.habit_logs add column if not exists value integer/
    );
    expect(SCHEMA).toMatch(
      /alter table public\.habit_logs add column if not exists completed_at text/
    );
  });

  it("adds every v3 habit definition column idempotently", () => {
    const columns = [
      "icon",
      "tracking_type",
      "target",
      "unit",
      "steps",
      "repeat_days",
      "times_of_day",
      "scheduled_at",
      "color",
      "motivation",
      "paused_at",
      "archived_at"
    ];

    columns.forEach((column) => {
      expect(
        new RegExp(`alter table public\\.habits add column if not exists ${column}\\b`).test(SCHEMA)
      ).toBe(true);
    });
  });
});

describe("schema.sql — function signature hygiene", () => {
  it("drops the old signature before re-creating a widened function", () => {
    // `create or replace` with a different argument list OVERLOADS instead of
    // replacing. Two overloads + a named-argument call = 42725.
    const widened = [
      "public.apply_habit_log(text, date, boolean, timestamptz)",
      "public.upsert_habit(text, text, text, numeric, boolean, text, integer, timestamptz, boolean)"
    ];

    widened.forEach((signature) => {
      const dropIndex = SCHEMA.indexOf(`drop function if exists ${signature}`);
      const name = signature.slice("public.".length, signature.indexOf("("));
      const createIndex = SCHEMA.indexOf(`create or replace function public.${name}(`);

      expect(dropIndex, `missing drop for ${signature}`).toBeGreaterThan(-1);
      expect(dropIndex, `drop of ${signature} must come before its create`).toBeLessThan(
        createIndex
      );
    });
  });

  it("every granted signature matches the function actually defined", () => {
    const defined = definedFunctions();
    const grants = SCHEMA.matchAll(/grant execute on function public\.(\w+)\s*\(([^)]*)\)/g);
    let checked = 0;

    for (const grant of grants) {
      const name = grant[1];
      const granted = grant[2]
        .split(",")
        .map((type) => type.trim())
        .filter((type) => type.length > 0);
      const actual = defined.get(name);

      if (!actual) continue; // granted on a table function defined elsewhere

      expect(granted, `grant signature for ${name}() is stale`).toEqual(actual);
      checked += 1;
    }

    expect(checked).toBeGreaterThan(4);
  });

  it("every sync RPC is revoked from public and anon", () => {
    ["apply_habit_log", "upsert_habit", "delete_habit", "get_sync_snapshot"].forEach((name) => {
      expect(
        new RegExp(`revoke execute on function public\\.${name}\\([^)]*\\) from public, anon`).test(
          SCHEMA
        ),
        `${name}() must be revoked from public, anon`
      ).toBe(true);
    });
  });
});
```

- [ ] **Step 2: Chạy test, xác nhận nó đỏ**

```bash
npx vitest run tests/schema-contract.test.ts
```

Expected: FAIL — chưa có cột nào, chưa có `drop function`.

> Nếu test `every granted signature matches` đỏ vì một hàm **có sẵn** (không phải hàm U1c đụng vào), đó là bug thật đang nằm sẵn trong repo: hàm đó đang được `EXECUTE` bởi `PUBLIC`. Sửa grant cho đúng chữ ký và ghi lại trong PR.

- [ ] **Step 3: Thêm cột mới vào `supabase/schema.sql`**

Chèn ngay sau dòng `alter table public.habit_logs add column if not exists mutated_at ...` (khoảng dòng 167):

```sql
-- habit_logs: v3 detail (spec §5.2 + UI/UX overhaul §9.3). `done` stays the
-- boolean truth — the client computes it with isEntryComplete because only the
-- client knows the tracking rule, and refresh_my_summary / shared_rhythms read
-- `done` directly. These two columns are additive detail, never a substitute.
--   value        NULL = written by a pre-v3 client (or an old schema); the
--                merge falls back to "a remote tick keeps the local value".
--   completed_at LOCAL "HH:mm" only. The day already lives in `date`, and
--                keeping it clock-only sidesteps timezone drift on the wire.
alter table public.habit_logs add column if not exists value integer;
alter table public.habit_logs add column if not exists completed_at text;

-- habits: the v3 definition (spec §5.1). Until now a habit that travelled
-- between devices arrived as a v2 shadow and got re-defaulted locally — the
-- other device never learned it was a 5-step checklist every Tuesday evening.
-- paused_at / archived_at are text, not date: the client compares them as
-- plain ISO labels (`date >= habit.pausedAt`) and never does date arithmetic
-- server-side, so text keeps a malformed value harmless instead of turning it
-- into a 22007 permanent-error drop that would silently lose a pause.
alter table public.habits add column if not exists icon text not null default '';
alter table public.habits add column if not exists tracking_type text not null default 'check';
alter table public.habits add column if not exists target numeric(8, 2) not null default 1;
alter table public.habits add column if not exists unit text;
alter table public.habits add column if not exists steps jsonb not null default '[]'::jsonb;
alter table public.habits add column if not exists repeat_days jsonb not null default '[1,2,3,4,5,6,7]'::jsonb;
alter table public.habits add column if not exists times_of_day jsonb not null default '["anytime"]'::jsonb;
alter table public.habits add column if not exists scheduled_at text;
alter table public.habits add column if not exists color text not null default 'clay';
alter table public.habits add column if not exists motivation text not null default '';
alter table public.habits add column if not exists paused_at text;
alter table public.habits add column if not exists archived_at text;
```

- [ ] **Step 4: Nới `apply_habit_log`**

Thay khối `create or replace function public.apply_habit_log(...)` (khoảng dòng 378-407) bằng:

```sql
-- Signature widened in U1c. `create or replace` with a new argument list would
-- OVERLOAD the old function, and PostgREST's named-argument call would then
-- fail with 42725 'function is not unique' — so the old one is dropped first.
-- Dropping also drops its grants; the new signature is granted below.
drop function if exists public.apply_habit_log(text, date, boolean, timestamptz);

-- Per-cell LWW upsert (spec §2.4): newer mutated_at wins; equal stamps -> tick
-- (done = true) wins. Stale writes are silently skipped, never overwrite.
-- p_value / p_completed_at ride along with the cell under the SAME stamp —
-- there is one LWW decision per cell, not one per column.
create or replace function public.apply_habit_log(
  p_habit_key text, p_date date, p_done boolean, p_mutated_at timestamptz,
  p_value integer default null, p_completed_at text default null)
returns void
language plpgsql
set search_path = public
as $$
declare
  v_habit public.habits%rowtype;
begin
  if auth.uid() is null then
    raise exception using errcode = 'P0001', message = 'not-authenticated';
  end if;
  if p_habit_key is null or p_date is null or p_done is null then
    raise exception using errcode = 'P0004', message = 'invalid-args';
  end if;
  select * into v_habit from public.habits
  where user_id = auth.uid() and key = p_habit_key;
  if not found then
    raise exception using errcode = 'P0002', message = 'habit-not-found';
  end if;
  if v_habit.deleted_at is not null then
    raise exception using errcode = 'P0003', message = 'habit-tombstoned';
  end if;
  insert into public.habit_logs as l
    (user_id, habit_id, date, done, mutated_at, value, completed_at)
  values
    (auth.uid(), v_habit.id, p_date, p_done, coalesce(p_mutated_at, now()),
     p_value, p_completed_at)
  on conflict (user_id, habit_id, date) do update
    set done = excluded.done,
        mutated_at = excluded.mutated_at,
        value = excluded.value,
        completed_at = excluded.completed_at
    where l.mutated_at < excluded.mutated_at
       or (l.mutated_at = excluded.mutated_at and excluded.done);
end;
$$;
```

- [ ] **Step 5: Nới `upsert_habit`**

Thay khối `create or replace function public.upsert_habit(...)` (khoảng dòng 415-467) bằng:

```sql
-- Signature widened in U1c — same overload hazard as apply_habit_log above.
drop function if exists public.upsert_habit(
  text, text, text, numeric, boolean, text, integer, timestamptz, boolean);

-- LWW habit upsert. Returns jsonb {status}: 'inserted' | 'updated' |
-- 'stale-skipped' | 'tombstone-blocked' | 'name-collision' (+ server:{key,name}).
-- p_expect_create = true marks a client-side CREATE: an existing live row with
-- a different name is then a slug collision (two devices invented the same
-- key for different habits) — the client must re-key, never merge silently.
-- The v3 block (p_icon .. p_archived_at) is written under the same
-- client_updated_at stamp as the v2 fields: one habit, one LWW decision.
create or replace function public.upsert_habit(
  p_key text, p_name text, p_category text, p_max_score numeric, p_active boolean,
  p_description text, p_sort_order integer, p_client_ts timestamptz,
  p_expect_create boolean default false,
  p_icon text default '', p_tracking_type text default 'check',
  p_target numeric default 1, p_unit text default null,
  p_steps jsonb default '[]'::jsonb,
  p_repeat_days jsonb default '[1,2,3,4,5,6,7]'::jsonb,
  p_times_of_day jsonb default '["anytime"]'::jsonb,
  p_scheduled_at text default null, p_color text default 'clay',
  p_motivation text default '', p_paused_at text default null,
  p_archived_at text default null)
returns jsonb
language plpgsql
set search_path = public
as $$
declare
  v_row public.habits%rowtype;
  v_ts timestamptz := coalesce(p_client_ts, now());
begin
  if auth.uid() is null then
    raise exception using errcode = 'P0001', message = 'not-authenticated';
  end if;
  if p_key is null or btrim(p_key) = '' or p_name is null or btrim(p_name) = '' then
    raise exception using errcode = 'P0004', message = 'invalid-habit';
  end if;
  select * into v_row from public.habits
  where user_id = auth.uid() and key = p_key;
  if not found then
    insert into public.habits
      (user_id, key, name, category, max_score, active, description, sort_order,
       client_updated_at, icon, tracking_type, target, unit, steps, repeat_days,
       times_of_day, scheduled_at, color, motivation, paused_at, archived_at)
    values
      (auth.uid(), p_key, p_name, coalesce(p_category, ''), coalesce(p_max_score, 1),
       coalesce(p_active, true), coalesce(p_description, ''), coalesce(p_sort_order, 0), v_ts,
       coalesce(p_icon, ''), coalesce(p_tracking_type, 'check'), coalesce(p_target, 1),
       p_unit, coalesce(p_steps, '[]'::jsonb),
       coalesce(p_repeat_days, '[1,2,3,4,5,6,7]'::jsonb),
       coalesce(p_times_of_day, '["anytime"]'::jsonb), p_scheduled_at,
       coalesce(p_color, 'clay'), coalesce(p_motivation, ''), p_paused_at, p_archived_at);
    return jsonb_build_object('status', 'inserted');
  end if;
  if p_expect_create and v_row.deleted_at is null and v_row.name <> p_name then
    return jsonb_build_object('status', 'name-collision',
      'server', jsonb_build_object('key', v_row.key, 'name', v_row.name));
  end if;
  if v_row.deleted_at is not null and v_row.deleted_at >= v_ts then
    return jsonb_build_object('status', 'tombstone-blocked');
  end if;
  if v_row.client_updated_at is not null and v_row.client_updated_at > v_ts then
    return jsonb_build_object('status', 'stale-skipped');
  end if;
  update public.habits
  set name = p_name,
      category = coalesce(p_category, ''),
      max_score = coalesce(p_max_score, 1),
      active = coalesce(p_active, true),
      description = coalesce(p_description, ''),
      sort_order = coalesce(p_sort_order, 0),
      icon = coalesce(p_icon, ''),
      tracking_type = coalesce(p_tracking_type, 'check'),
      target = coalesce(p_target, 1),
      unit = p_unit,
      steps = coalesce(p_steps, '[]'::jsonb),
      repeat_days = coalesce(p_repeat_days, '[1,2,3,4,5,6,7]'::jsonb),
      times_of_day = coalesce(p_times_of_day, '["anytime"]'::jsonb),
      scheduled_at = p_scheduled_at,
      color = coalesce(p_color, 'clay'),
      motivation = coalesce(p_motivation, ''),
      paused_at = p_paused_at,
      archived_at = p_archived_at,
      client_updated_at = v_ts,
      deleted_at = null -- re-create after tombstone: newer client_ts wins (§2.4)
  where user_id = auth.uid() and key = p_key;
  return jsonb_build_object('status', 'updated');
end;
$$;
```

- [ ] **Step 6: Nới `get_sync_snapshot`**

Thay khối `create or replace function public.get_sync_snapshot()` (khoảng dòng 776-808) bằng:

```sql
create or replace function public.get_sync_snapshot()
returns jsonb
language sql
stable
set search_path = public
as $$
  select jsonb_build_object(
    'habits', coalesce((
      select jsonb_agg(jsonb_build_object(
        'key', h.key,
        'name', h.name,
        'category', h.category,
        'maxScore', h.max_score,
        'active', h.active,
        'description', h.description,
        'sortOrder', h.sort_order,
        'clientUpdatedAt', h.client_updated_at,
        'deletedAt', h.deleted_at,
        'icon', h.icon,
        'trackingType', h.tracking_type,
        'target', h.target,
        'unit', h.unit,
        'steps', h.steps,
        'repeatDays', h.repeat_days,
        'timesOfDay', h.times_of_day,
        'scheduledAt', h.scheduled_at,
        'color', h.color,
        'motivation', h.motivation,
        'pausedAt', h.paused_at,
        'archivedAt', h.archived_at) order by h.sort_order)
      from public.habits h
      where h.user_id = auth.uid()), '[]'::jsonb),
    'logs', coalesce((
      select jsonb_agg(jsonb_build_object(
        'habitKey', h.key,
        'date', l.date,
        'done', l.done,
        'mutatedAt', l.mutated_at,
        'value', l.value,
        'completedAt', l.completed_at))
      from public.habit_logs l
      join public.habits h on h.id = l.habit_id and h.user_id = l.user_id
      where l.user_id = auth.uid()), '[]'::jsonb),
    'companion', public.companion_state_jsonb(),
    'serverTime', now()
  );
$$;
```

- [ ] **Step 7: Cập nhật grant cho hai chữ ký mới**

Trong khối grants (khoảng dòng 820-824), thay hai cặp `apply_habit_log` / `upsert_habit` bằng:

```sql
revoke execute on function public.apply_habit_log(text, date, boolean, timestamptz, integer, text) from public, anon;
grant execute on function public.apply_habit_log(text, date, boolean, timestamptz, integer, text) to authenticated;
revoke execute on function public.upsert_habit(text, text, text, numeric, boolean, text, integer, timestamptz, boolean, text, text, numeric, text, jsonb, jsonb, jsonb, text, text, text, text, text) from public, anon;
grant execute on function public.upsert_habit(text, text, text, numeric, boolean, text, integer, timestamptz, boolean, text, text, numeric, text, jsonb, jsonb, jsonb, text, text, text, text, text) to authenticated;
```

- [ ] **Step 8: Chạy test, xác nhận xanh**

```bash
npx vitest run tests/schema-contract.test.ts
```

Expected: PASS (4 test).

- [ ] **Step 9: Commit**

```bash
git add supabase/schema.sql tests/schema-contract.test.ts
git commit -m "feat(u1c): habit_logs and habits carry the v3 shape"
```

---

### Task 2: Hợp đồng sync — mutation và snapshot mang payload v3

**Files:**
- Modify: `src/lib/sync/types.ts:17-49` (`SetHabitLogMutation`, `SyncHabitPayload`), `:116-134` (`ServerHabit`, `ServerHabitLog`)
- Test: `src/lib/sync/queue.test.ts`

**Interfaces:**
- Consumes: `HabitColor`, `TimeOfDay`, `TrackingType` từ `@/components/dashboard/habit-model`.
- Produces:
  - `SetHabitLogMutation` thêm `value?: number` và `completedAt?: string`.
  - `SyncHabitPayload` thêm `icon: string`, `trackingType: TrackingType`, `target: number`, `unit: string | null`, `steps: string[]`, `repeatDays: number[]`, `timesOfDay: TimeOfDay[]`, `scheduledAt: string | null`, `color: HabitColor`, `motivation: string`, `pausedAt: string | null`, `archivedAt: string | null`.
  - `ServerHabit` thêm đúng 12 field trên (cùng tên, cùng kiểu).
  - `ServerHabitLog` thêm `value: number | null` và `completedAt: string | null`.

- [ ] **Step 1: Viết test đỏ trong `src/lib/sync/queue.test.ts`**

Thêm vào cuối file:

```ts
describe("coalescing keeps the v3 detail", () => {
  it("a newer write for the same cell replaces the older one, value and all", () => {
    const older: SyncMutation = {
      kind: "setHabitLog",
      habitKey: "water",
      date: "2026-07-27",
      done: false,
      value: 3,
      clientTs: "2026-07-27T01:00:00.000Z"
    };
    const newer: SyncMutation = {
      kind: "setHabitLog",
      habitKey: "water",
      date: "2026-07-27",
      done: true,
      value: 8,
      completedAt: "21:40",
      clientTs: "2026-07-27T02:00:00.000Z"
    };

    const queue = coalesceEnqueue(coalesceEnqueue([], older), newer);

    expect(queue).toEqual([newer]);
  });

  it("an out-of-order older write never downgrades the queued value", () => {
    const newer: SyncMutation = {
      kind: "setHabitLog",
      habitKey: "water",
      date: "2026-07-27",
      done: true,
      value: 8,
      completedAt: "21:40",
      clientTs: "2026-07-27T02:00:00.000Z"
    };
    const older: SyncMutation = {
      kind: "setHabitLog",
      habitKey: "water",
      date: "2026-07-27",
      done: false,
      value: 3,
      clientTs: "2026-07-27T01:00:00.000Z"
    };

    expect(coalesceEnqueue(coalesceEnqueue([], newer), older)).toEqual([newer]);
  });
});
```

- [ ] **Step 2: Chạy test, xác nhận đỏ**

```bash
npx vitest run src/lib/sync/queue.test.ts
```

Expected: FAIL — TypeScript báo `value`/`completedAt` không tồn tại trên `SetHabitLogMutation`.

- [ ] **Step 3: Nới các type trong `src/lib/sync/types.ts`**

Thêm import ở đầu file:

```ts
import type {
  HabitColor,
  TimeOfDay,
  TrackingType
} from "@/components/dashboard/habit-model";
```

Sửa `SetHabitLogMutation`:

```ts
export type SetHabitLogMutation = {
  kind: "setHabitLog";
  habitKey: string;
  /** YYYY-MM-DD */
  date: string;
  /** SET the value — never a flip. Still the boolean truth: the client owns
   * the tracking rule, so it decides done, not the server. */
  done: boolean;
  /**
   * The raw v3 reading behind `done` (glasses, minutes, checklist bitmask).
   * Absent on a mutation minted before U1c — the server stores NULL and the
   * merge falls back to "a remote tick keeps whatever this device had".
   */
  value?: number;
  /** LOCAL "HH:mm". The day is already in `date`; clock-only dodges tz drift. */
  completedAt?: string;
  /** ISO client stamp; also the LWW stamp for this cell (shadow map). */
  clientTs: string;
};
```

Sửa `SyncHabitPayload` — thêm khối v3 sau `sortOrder`:

```ts
export type SyncHabitPayload = {
  key: string;
  name: string;
  category: string;
  maxScore: number;
  active: boolean;
  description: string;
  sortOrder: number;
  // ---- v3 definition (spec §5.1). Before U1c a habit crossing devices
  // arrived as a v2 shadow and was re-defaulted locally; the other device
  // never learned it was a 5-step checklist on Tuesday evenings. ----
  icon: string;
  trackingType: TrackingType;
  target: number;
  unit: string | null;
  steps: string[];
  repeatDays: number[];
  timesOfDay: TimeOfDay[];
  scheduledAt: string | null;
  color: HabitColor;
  motivation: string;
  pausedAt: string | null;
  archivedAt: string | null;
};
```

Sửa `ServerHabit` và `ServerHabitLog`:

```ts
export type ServerHabit = {
  key: string;
  name: string;
  category: string;
  maxScore: number;
  active: boolean;
  description: string;
  sortOrder: number;
  clientUpdatedAt: string | null;
  deletedAt: string | null;
  icon: string;
  trackingType: TrackingType;
  target: number;
  unit: string | null;
  steps: string[];
  repeatDays: number[];
  timesOfDay: TimeOfDay[];
  scheduledAt: string | null;
  color: HabitColor;
  motivation: string;
  pausedAt: string | null;
  archivedAt: string | null;
};

export type ServerHabitLog = {
  habitKey: string;
  /** YYYY-MM-DD */
  date: string;
  done: boolean;
  mutatedAt: string;
  /** null = row written before U1c, or by a client that had no value to send. */
  value: number | null;
  /** LOCAL "HH:mm", null when never completed. */
  completedAt: string | null;
};
```

- [ ] **Step 4: Chạy test + typecheck**

```bash
npx vitest run src/lib/sync/queue.test.ts && npx tsc --noEmit
```

Expected: test PASS. `tsc` sẽ báo lỗi ở `merge.ts` / `sync-actions.ts` / `importer.ts` / `merge.test.ts` vì các object literal `ServerHabit`/`ServerHabitLog` giờ thiếu field — **đó là dự kiến**, Task 3-6 vá. Ghi lại danh sách file lỗi để đối chiếu.

- [ ] **Step 5: Commit**

```bash
git add src/lib/sync/types.ts src/lib/sync/queue.test.ts
git commit -m "feat(u1c): the sync contract carries v3 log detail and habit fields"
```

> **Lưu ý cho người thực thi:** commit này cố ý để cây typecheck đỏ ở các file hạ nguồn. Nếu thấy khó chịu, gộp Task 2+3+4 thành một commit — nhưng đừng gộp thêm Task 5 (server action) vào, để reviewer soi riêng phần đụng RPC.

---

### Task 3: merge — ô log mang `value`/`completedAt`, chịu được server cũ

**Files:**
- Modify: `src/lib/sync/merge.ts:223-249` (vòng `server.logs.forEach`)
- Test: `src/lib/sync/merge.test.ts`

**Interfaces:**
- Consumes: `ServerHabitLog` (đã có `value: number | null`, `completedAt: string | null`) từ Task 2.
- Produces: không có API mới — hành vi của `mergeServerIntoLocal` đổi.

Quy tắc: **một stamp LWW cho cả ô**. Server thắng ⇒ lấy trọn `value` + `completedAt` + `done` của server. Server thắng nhưng `value === null` (schema cũ) ⇒ giữ nguyên hành vi hôm nay.

- [ ] **Step 1: Viết test đỏ trong `src/lib/sync/merge.test.ts`**

Thêm vào cuối file (helper `makeState` / `makeSnapshot` đã có sẵn trong file — đọc phần đầu file để dùng đúng tên; nếu không có, dựng state bằng `createInitialDashboardState`):

```ts
describe("log cells carry the v3 reading (U1c)", () => {
  it("a newer server cell replaces value and completedAt wholesale", () => {
    const local = {
      ...createInitialDashboardState("2026-07-27"),
      records: {
        "2026-07-27": {
          date: "2026-07-27",
          entries: { wake_up: { value: 1, completedAt: "06:10" } },
          completions: { wake_up: true }
        }
      }
    };
    const server = {
      habits: [],
      logs: [
        {
          habitKey: "wake_up",
          date: "2026-07-27",
          done: true,
          mutatedAt: "2026-07-27T09:00:00.000Z",
          value: 1,
          completedAt: "05:45"
        }
      ],
      companion: null
    };
    const shadow = { "2026-07-27": { wake_up: "2026-07-27T08:00:00.000Z" } };

    const result = mergeServerIntoLocal(local, server, shadow, null);

    expect(result.state.records["2026-07-27"].entries.wake_up).toEqual({
      value: 1,
      completedAt: "05:45"
    });
  });

  it("a newer server cell brings partial progress across", () => {
    const local = createInitialDashboardState("2026-07-27");
    const server = {
      habits: [],
      logs: [
        {
          habitKey: "wake_up",
          date: "2026-07-27",
          done: false,
          mutatedAt: "2026-07-27T09:00:00.000Z",
          value: 4,
          completedAt: null
        }
      ],
      companion: null
    };

    const result = mergeServerIntoLocal(local, server, {}, null);

    expect(result.state.records["2026-07-27"].entries.wake_up).toEqual({ value: 4 });
    expect(result.state.records["2026-07-27"].completions.wake_up).toBe(false);
  });

  it("an old server (value null) still never downgrades a richer local cell", () => {
    // Deployment order is not guaranteed: the app can ship before the owner
    // applies schema.sql. A tick from such a server must not turn 8 glasses
    // into a bare 1.
    const local = {
      ...createInitialDashboardState("2026-07-27"),
      records: {
        "2026-07-27": {
          date: "2026-07-27",
          entries: { wake_up: { value: 8, completedAt: "21:40" } },
          completions: { wake_up: true }
        }
      }
    };
    const server = {
      habits: [],
      logs: [
        {
          habitKey: "wake_up",
          date: "2026-07-27",
          done: true,
          mutatedAt: "2026-07-27T09:00:00.000Z",
          value: null,
          completedAt: null
        }
      ],
      companion: null
    };

    const result = mergeServerIntoLocal(local, server, {}, null);

    expect(result.state.records["2026-07-27"].entries.wake_up).toEqual({
      value: 8,
      completedAt: "21:40"
    });
  });

  it("a remote untick still clears the cell", () => {
    const local = {
      ...createInitialDashboardState("2026-07-27"),
      records: {
        "2026-07-27": {
          date: "2026-07-27",
          entries: { wake_up: { value: 1, completedAt: "06:10" } },
          completions: { wake_up: true }
        }
      }
    };
    const server = {
      habits: [],
      logs: [
        {
          habitKey: "wake_up",
          date: "2026-07-27",
          done: false,
          mutatedAt: "2026-07-27T09:00:00.000Z",
          value: 0,
          completedAt: null
        }
      ],
      companion: null
    };

    const result = mergeServerIntoLocal(local, server, {}, null);

    expect(result.state.records["2026-07-27"].entries.wake_up).toEqual({ value: 0 });
    expect(result.state.records["2026-07-27"].completions.wake_up).toBe(false);
  });
});
```

> Ba test hiện có đang dựng `ServerHabitLog` không có `value`/`completedAt` — thêm `value: null, completedAt: null` vào chúng để `tsc` xanh. Đừng đổi kỳ vọng của chúng: hành vi cũ phải được giữ nguyên.

- [ ] **Step 2: Chạy test, xác nhận đỏ**

```bash
npx vitest run src/lib/sync/merge.test.ts
```

Expected: FAIL — `entries.wake_up` vẫn là `{ value: 1 }` cũ, `completedAt` không được mang sang.

- [ ] **Step 3: Sửa vòng merge log trong `src/lib/sync/merge.ts`**

Thay khối trong `server.logs.forEach` (từ `const record = nextRecords[log.date] ...` đến hết object `nextRecords[log.date] = {...}`) bằng:

```ts
    const record = nextRecords[log.date] ?? { date: log.date, entries: {}, completions: {} };
    const previous = record.entries[log.habitKey];

    nextRecords[log.date] = {
      date: log.date,
      entries: { ...record.entries, [log.habitKey]: serverEntry(log, previous) },
      completions: { ...record.completions, [log.habitKey]: log.done }
    };
```

Thêm helper cạnh `toDashboardHabit` (cuối file):

```ts
/**
 * The cell the server just won, as a LogEntry. One LWW stamp covers the whole
 * cell, so when the server wins it wins value + completedAt + done together.
 *
 * `value === null` means the row predates U1c (or the deployed schema does):
 * the server genuinely does not know the reading, only the boolean. Then we
 * keep the old contract — a remote tick preserves whatever richer value this
 * device already had, so "8 glasses" is never flattened to a bare 1.
 */
function serverEntry(log: ServerHabitLog, previous: LogEntry | undefined): LogEntry {
  if (log.value === null) return log.done ? (previous ?? { value: 1 }) : { value: 0 };

  const entry: LogEntry = { value: Math.max(0, Math.trunc(log.value)) };

  if (log.completedAt) entry.completedAt = log.completedAt;

  return entry;
}
```

Thêm `ServerHabitLog` vào khối `import type { ... } from "./types"` và `LogEntry` vào import từ `@/components/dashboard/habit-model`:

```ts
import { type LogEntry } from "@/components/dashboard/habit-model";
```

Sửa comment cũ ở trên vòng lặp (dòng ~234-236) — nó đang nói "The server still speaks boolean (U1c widens the contract)", giờ đã sai:

```ts
    // One LWW stamp per cell: winning brings value, completedAt and done
    // across together. serverEntry() handles a server that only knows done.
```

- [ ] **Step 4: Chạy test, xác nhận xanh**

```bash
npx vitest run src/lib/sync/merge.test.ts
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/lib/sync/merge.ts src/lib/sync/merge.test.ts
git commit -m "feat(u1c): a synced log cell carries its reading, not just a tick"
```

---

### Task 4: merge — habit v3 đi được cả hai chiều

**Files:**
- Modify: `src/lib/sync/merge.ts:778-801` (`toDashboardHabit`, `applyServerHabitFields`)
- Test: `src/lib/sync/merge.test.ts`

**Interfaces:**
- Consumes: `ServerHabit` với 12 field v3 (Task 2).
- Produces: không có API mới.

Nguyên tắc: `migrateHabitFields` vẫn là cửa duy nhất — nó đã biết chuẩn hoá/sửa mọi field v3 (`normalizeTimesOfDay`, `normalizeRepeatDays`, `isTrackingType`, ...). Truyền field server **thô** vào nó, đừng tự validate lần hai.

- [ ] **Step 1: Viết test đỏ trong `src/lib/sync/merge.test.ts`**

```ts
describe("habit v3 definition crosses devices (U1c)", () => {
  const serverHabit = {
    key: "read",
    name: "Đọc sách",
    category: "Growth",
    maxScore: 1,
    active: true,
    description: "",
    sortOrder: 0,
    clientUpdatedAt: "2026-07-27T10:00:00.000Z",
    deletedAt: null,
    icon: "📚",
    trackingType: "duration" as const,
    target: 20,
    unit: null,
    steps: [],
    repeatDays: [2, 4, 6],
    timesOfDay: ["evening" as const],
    scheduledAt: "21:00",
    color: "dusk" as const,
    motivation: "Mỗi tối một chương",
    pausedAt: null,
    archivedAt: null
  };

  it("a habit adopted from the server arrives fully v3, not re-defaulted", () => {
    const result = mergeServerIntoLocal(
      createInitialDashboardState("2026-07-27"),
      { habits: [serverHabit], logs: [], companion: null },
      {},
      null
    );
    const adopted = result.state.habits.find((habit) => habit.id === "read");

    expect(adopted).toBeTruthy();
    expect(adopted?.trackingType).toBe("duration");
    expect(adopted?.target).toBe(20);
    expect(adopted?.repeatDays).toEqual([2, 4, 6]);
    expect(adopted?.timesOfDay).toEqual(["evening"]);
    expect(adopted?.scheduledAt).toBe("21:00");
    expect(adopted?.color).toBe("dusk");
    expect(adopted?.icon).toBe("📚");
    expect(adopted?.motivation).toBe("Mỗi tối một chương");
  });

  it("a newer server edit overwrites the local v3 fields", () => {
    const local = createInitialDashboardState("2026-07-27");
    const edited = {
      ...serverHabit,
      key: local.habits[0].id,
      name: local.habits[0].name,
      trackingType: "count" as const,
      target: 5,
      unit: "lần",
      repeatDays: [1, 3, 5],
      pausedAt: "2026-07-28"
    };

    const result = mergeServerIntoLocal(
      local,
      { habits: [edited], logs: [], companion: null },
      {},
      null
    );
    const merged = result.state.habits.find((habit) => habit.id === edited.key);

    expect(merged?.trackingType).toBe("count");
    expect(merged?.target).toBe(5);
    expect(merged?.unit).toBe("lần");
    expect(merged?.repeatDays).toEqual([1, 3, 5]);
    expect(merged?.pausedAt).toBe("2026-07-28");
  });

  it("garbage from the wire is normalised, never trusted raw", () => {
    const junk = {
      ...serverHabit,
      trackingType: "telepathy" as unknown as "check",
      repeatDays: [] as number[],
      timesOfDay: [] as never[],
      color: "neon" as unknown as "clay"
    };

    const result = mergeServerIntoLocal(
      createInitialDashboardState("2026-07-27"),
      { habits: [junk], logs: [], companion: null },
      {},
      null
    );
    const adopted = result.state.habits.find((habit) => habit.id === "read");

    expect(adopted?.trackingType).toBe("check");
    expect(adopted?.repeatDays).toEqual([1, 2, 3, 4, 5, 6, 7]);
    expect(adopted?.timesOfDay).toEqual(["anytime"]);
    expect(adopted?.color).toBe("clay");
  });
});
```

- [ ] **Step 2: Chạy test, xác nhận đỏ**

```bash
npx vitest run src/lib/sync/merge.test.ts
```

Expected: FAIL — `trackingType` là `"check"` (default), `target` là 1.

- [ ] **Step 3: Sửa hai helper trong `src/lib/sync/merge.ts`**

```ts
/**
 * A habit arriving from another device. The server fields go into
 * migrateHabitFields RAW: it is the single place that knows how to normalise
 * every v3 field (unknown tracking type -> check, empty repeatDays -> all
 * seven, "Cả ngày" exclusivity), so validating here as well would be a second
 * rule set to keep in step.
 */
function toDashboardHabit(habit: ServerHabit): DashboardHabit {
  return migrateHabitFields({
    id: habit.key,
    key: habit.key,
    name: habit.name,
    category: habit.category,
    maxScore: habit.maxScore,
    description: habit.description,
    iconName: habitIcon(habit.key, habit.category),
    ...serverV3Fields(habit)
  });
}

function applyServerHabitFields(local: DashboardHabit, server: ServerHabit): DashboardHabit {
  return migrateHabitFields({
    ...local,
    name: server.name,
    category: server.category,
    maxScore: server.maxScore,
    description: server.description,
    iconName: habitIcon(local.key, server.category),
    ...serverV3Fields(server)
  });
}

/** The v3 slice of a server habit, still raw — migrateHabitFields normalises. */
function serverV3Fields(habit: ServerHabit) {
  return {
    icon: habit.icon,
    trackingType: habit.trackingType,
    target: habit.target,
    unit: habit.unit ?? undefined,
    steps: habit.steps,
    repeatDays: habit.repeatDays,
    timesOfDay: habit.timesOfDay,
    scheduledAt: habit.scheduledAt ?? undefined,
    color: habit.color,
    motivation: habit.motivation,
    pausedAt: habit.pausedAt ?? undefined,
    archivedAt: habit.archivedAt ?? undefined
  };
}
```

- [ ] **Step 4: Chạy toàn bộ test sync, xác nhận xanh**

```bash
npx vitest run src/lib/sync && npx tsc --noEmit
```

Expected: test PASS. `tsc` còn lỗi ở `sync-actions.ts` và `importer.ts` — Task 5, 6 vá.

- [ ] **Step 5: Commit**

```bash
git add src/lib/sync/merge.ts src/lib/sync/merge.test.ts
git commit -m "feat(u1c): a habit crossing devices keeps its v3 definition"
```

---

### Task 5: Server action — truyền tham số mới, parse cột mới, PGRST202 là retry

**Files:**
- Modify: `src/lib/server/sync-actions.ts:36-46` (`PERMANENT_ERROR_CODES` + comment), `:138-160` (`applyMutation`), `:360-395` (`parseHabit`, `parseLog`)
- Create: `src/lib/server/sync-actions.test.ts`

**Interfaces:**
- Consumes: `SetHabitLogMutation.value/completedAt`, `SyncHabitPayload` v3, `ServerHabit`/`ServerHabitLog` v3 (Task 2).
- Produces: không có API mới — `pushMutations` và `fetchSyncSnapshot` giữ nguyên chữ ký.

- [ ] **Step 1: Viết test đỏ — tạo `src/lib/server/sync-actions.test.ts`**

```ts
import { beforeEach, describe, expect, it, vi } from "vitest";

import { fetchSyncSnapshot, pushMutations } from "@/lib/server/sync-actions";
import type { SyncHabitPayload } from "@/lib/sync/types";

const supabaseMocks = vi.hoisted(() => ({
  getUser: vi.fn(),
  rpc: vi.fn()
}));

vi.mock("@/lib/supabase/server", () => ({
  createClient: vi.fn(async () => ({
    auth: { getUser: supabaseMocks.getUser },
    rpc: supabaseMocks.rpc
  }))
}));

const HABIT: SyncHabitPayload = {
  key: "read",
  name: "Đọc sách",
  category: "Growth",
  maxScore: 1,
  active: true,
  description: "",
  sortOrder: 0,
  icon: "📚",
  trackingType: "duration",
  target: 20,
  unit: null,
  steps: [],
  repeatDays: [2, 4, 6],
  timesOfDay: ["evening"],
  scheduledAt: "21:00",
  color: "dusk",
  motivation: "Mỗi tối một chương",
  pausedAt: null,
  archivedAt: null
};

beforeEach(() => {
  vi.clearAllMocks();
  supabaseMocks.getUser.mockResolvedValue({
    data: { user: { id: "11111111-0000-4000-8000-000000000000" } },
    error: null
  });
  supabaseMocks.rpc.mockResolvedValue({ data: { status: "updated" }, error: null });
});

describe("pushMutations sends the v3 payload", () => {
  it("a log mutation carries value and completedAt", async () => {
    await pushMutations(
      [
        {
          kind: "setHabitLog",
          habitKey: "water",
          date: "2026-07-27",
          done: true,
          value: 8,
          completedAt: "21:40",
          clientTs: "2026-07-27T14:40:00.000Z"
        }
      ],
      null
    );

    expect(supabaseMocks.rpc).toHaveBeenCalledWith("apply_habit_log", {
      p_habit_key: "water",
      p_date: "2026-07-27",
      p_done: true,
      p_mutated_at: "2026-07-27T14:40:00.000Z",
      p_value: 8,
      p_completed_at: "21:40"
    });
  });

  it("a mutation minted before U1c sends nulls, not undefined", async () => {
    await pushMutations(
      [
        {
          kind: "setHabitLog",
          habitKey: "water",
          date: "2026-07-27",
          done: true,
          clientTs: "2026-07-27T14:40:00.000Z"
        }
      ],
      null
    );

    expect(supabaseMocks.rpc).toHaveBeenCalledWith(
      "apply_habit_log",
      expect.objectContaining({ p_value: null, p_completed_at: null })
    );
  });

  it("an upsert carries every v3 definition field", async () => {
    await pushMutations(
      [{ kind: "upsertHabit", habit: HABIT, clientTs: "2026-07-27T10:00:00.000Z" }],
      null
    );

    expect(supabaseMocks.rpc).toHaveBeenCalledWith(
      "upsert_habit",
      expect.objectContaining({
        p_icon: "📚",
        p_tracking_type: "duration",
        p_target: 20,
        p_unit: null,
        p_steps: [],
        p_repeat_days: [2, 4, 6],
        p_times_of_day: ["evening"],
        p_scheduled_at: "21:00",
        p_color: "dusk",
        p_motivation: "Mỗi tối một chương",
        p_paused_at: null,
        p_archived_at: null
      })
    );
  });
});

describe("a server still on the old schema", () => {
  it("PGRST202 retries instead of dropping the mutation", async () => {
    // The app can ship before the owner applies schema.sql. Dropping here
    // would lose the write for good; retrying self-heals the moment the SQL
    // lands.
    supabaseMocks.rpc.mockResolvedValue({
      data: null,
      error: { code: "PGRST202", message: "Could not find the function" }
    });

    const result = await pushMutations(
      [
        {
          kind: "setHabitLog",
          habitKey: "water",
          date: "2026-07-27",
          done: true,
          value: 8,
          clientTs: "2026-07-27T14:40:00.000Z"
        }
      ],
      null
    );

    expect(result).toMatchObject({ ok: true, outcomes: [{ status: "retry" }] });
  });
});

describe("fetchSyncSnapshot parses the v3 columns", () => {
  it("reads value, completedAt and the habit definition", async () => {
    supabaseMocks.rpc.mockResolvedValue({
      data: {
        habits: [
          {
            key: "read",
            name: "Đọc sách",
            category: "Growth",
            maxScore: 1,
            active: true,
            description: "",
            sortOrder: 0,
            clientUpdatedAt: null,
            deletedAt: null,
            icon: "📚",
            trackingType: "duration",
            target: 20,
            unit: null,
            steps: [],
            repeatDays: [2, 4, 6],
            timesOfDay: ["evening"],
            scheduledAt: "21:00",
            color: "dusk",
            motivation: "Mỗi tối một chương",
            pausedAt: null,
            archivedAt: null
          }
        ],
        logs: [
          {
            habitKey: "read",
            date: "2026-07-27",
            done: true,
            mutatedAt: "2026-07-27T14:40:00.000Z",
            value: 20,
            completedAt: "21:40"
          }
        ],
        companion: null,
        serverTime: "2026-07-27T15:00:00.000Z"
      },
      error: null
    });

    const result = await fetchSyncSnapshot();

    expect(result).toMatchObject({ ok: true });

    if (!result.ok) throw new Error("expected a snapshot");

    expect(result.snapshot.habits[0]).toMatchObject({
      trackingType: "duration",
      target: 20,
      repeatDays: [2, 4, 6],
      timesOfDay: ["evening"],
      color: "dusk"
    });
    expect(result.snapshot.logs[0]).toMatchObject({ value: 20, completedAt: "21:40" });
  });

  it("an old snapshot without the v3 columns still parses, with safe defaults", () => {
    // Not a hypothetical: the deployed schema lags the deployed app.
    supabaseMocks.rpc.mockResolvedValue({
      data: {
        habits: [
          {
            key: "read",
            name: "Đọc sách",
            category: "Growth",
            maxScore: 1,
            active: true,
            description: "",
            sortOrder: 0,
            clientUpdatedAt: null,
            deletedAt: null
          }
        ],
        logs: [
          {
            habitKey: "read",
            date: "2026-07-27",
            done: true,
            mutatedAt: "2026-07-27T14:40:00.000Z"
          }
        ],
        companion: null
      },
      error: null
    });

    return fetchSyncSnapshot().then((result) => {
      if (!result.ok) throw new Error("expected a snapshot");

      expect(result.snapshot.habits[0].trackingType).toBe("check");
      expect(result.snapshot.habits[0].repeatDays).toEqual([1, 2, 3, 4, 5, 6, 7]);
      expect(result.snapshot.habits[0].timesOfDay).toEqual(["anytime"]);
      expect(result.snapshot.logs[0].value).toBeNull();
      expect(result.snapshot.logs[0].completedAt).toBeNull();
    });
  });
});
```

- [ ] **Step 2: Chạy test, xác nhận đỏ**

```bash
npx vitest run src/lib/server/sync-actions.test.ts
```

Expected: FAIL — RPC được gọi thiếu `p_value`, `p_icon`, ...; snapshot thiếu field v3.

- [ ] **Step 3: Truyền tham số mới trong `applyMutation`**

`case "setHabitLog"`:

```ts
      case "setHabitLog": {
        const { error } = await supabase.rpc("apply_habit_log", {
          p_habit_key: mutation.habitKey,
          p_date: mutation.date,
          p_done: mutation.done,
          p_mutated_at: mutation.clientTs,
          // Explicit null, not undefined: a key dropped from the JSON body
          // makes PostgREST fall back to the SQL default, which for a WIDENED
          // function is fine — but an explicit null keeps the wire shape
          // stable and the test honest about what the server receives.
          p_value: mutation.value ?? null,
          p_completed_at: mutation.completedAt ?? null
        });

        return error ? classifyRpcError(error) : { status: "applied" };
      }
```

`case "upsertHabit"`:

```ts
      case "upsertHabit": {
        const { data, error } = await supabase.rpc("upsert_habit", {
          p_key: mutation.habit.key,
          p_name: mutation.habit.name,
          p_category: mutation.habit.category,
          p_max_score: mutation.habit.maxScore,
          p_active: mutation.habit.active,
          p_description: mutation.habit.description,
          p_sort_order: mutation.habit.sortOrder,
          p_client_ts: mutation.clientTs,
          p_expect_create: mutation.expectCreate === true,
          p_icon: mutation.habit.icon,
          p_tracking_type: mutation.habit.trackingType,
          p_target: mutation.habit.target,
          p_unit: mutation.habit.unit,
          p_steps: mutation.habit.steps,
          p_repeat_days: mutation.habit.repeatDays,
          p_times_of_day: mutation.habit.timesOfDay,
          p_scheduled_at: mutation.habit.scheduledAt,
          p_color: mutation.habit.color,
          p_motivation: mutation.habit.motivation,
          p_paused_at: mutation.habit.pausedAt,
          p_archived_at: mutation.habit.archivedAt
        });
```

(phần còn lại của case giữ nguyên)

- [ ] **Step 4: Ghi rõ PGRST202 là retry**

Sửa comment trên `PERMANENT_ERROR_CODES` — thêm đoạn:

```ts
// PGRST202 ("could not find the function") is deliberately NOT here. It means
// the deployed schema is older than the deployed app — schema.sql has not been
// applied yet. Dropping the mutation would lose the write for good; retrying
// self-heals the moment the owner runs the SQL.
```

- [ ] **Step 5: Parse cột mới trong `parseHabit` / `parseLog`**

Thêm helper cạnh `booleanMap` (bên trên `parseHabit`):

```ts
function stringArray(value: Json | undefined): string[] {
  return Array.isArray(value) ? value.filter((item): item is string => typeof item === "string") : [];
}

function numberArray(value: Json | undefined): number[] {
  return Array.isArray(value)
    ? value.filter((item): item is number => typeof item === "number" && Number.isFinite(item))
    : [];
}

function nullableNumber(value: Json | undefined): number | null {
  return typeof value === "number" && Number.isFinite(value) ? value : null;
}
```

Sửa `parseHabit` — thay khối `return {...}` bằng:

```ts
  // The v3 block tolerates an old deployed schema: a missing column parses to
  // the same default migrateHabitFields would have applied anyway, so an
  // app-ahead-of-schema deploy degrades to exactly the pre-U1c behaviour.
  const repeatDays = numberArray(obj.repeatDays);
  const timesOfDay = stringArray(obj.timesOfDay);

  return {
    key,
    name,
    category: asString(obj.category) ?? "",
    maxScore: asNumber(obj.maxScore, 1),
    active: asBoolean(obj.active, true),
    description: asString(obj.description) ?? "",
    sortOrder: asNumber(obj.sortOrder, 0),
    clientUpdatedAt: asString(obj.clientUpdatedAt),
    deletedAt: asString(obj.deletedAt),
    icon: asString(obj.icon) ?? "",
    trackingType: (asString(obj.trackingType) ?? "check") as ServerHabit["trackingType"],
    target: asNumber(obj.target, 1),
    unit: asString(obj.unit),
    steps: stringArray(obj.steps),
    repeatDays: repeatDays.length > 0 ? repeatDays : [1, 2, 3, 4, 5, 6, 7],
    timesOfDay: (timesOfDay.length > 0
      ? timesOfDay
      : ["anytime"]) as ServerHabit["timesOfDay"],
    scheduledAt: asString(obj.scheduledAt),
    color: (asString(obj.color) ?? "clay") as ServerHabit["color"],
    motivation: asString(obj.motivation) ?? "",
    pausedAt: asString(obj.pausedAt),
    archivedAt: asString(obj.archivedAt)
  };
```

Sửa `parseLog` — thay dòng `return {...}` cuối bằng:

```ts
  return {
    habitKey,
    date,
    done: asBoolean(obj.done, false),
    mutatedAt,
    value: nullableNumber(obj.value),
    completedAt: asString(obj.completedAt)
  };
```

> Ép kiểu `as ServerHabit["trackingType"]` là cố ý: đây là biên giới dây, `migrateHabitFields` ở tầng merge mới là nơi kiểm tra giá trị thật. Đừng nhân đôi luật hợp lệ ở đây.

- [ ] **Step 6: Chạy test, xác nhận xanh**

```bash
npx vitest run src/lib/server/sync-actions.test.ts && npx tsc --noEmit
```

Expected: test PASS; `tsc` chỉ còn lỗi ở `importer.ts` (Task 6).

- [ ] **Step 7: Commit**

```bash
git add src/lib/server/sync-actions.ts src/lib/server/sync-actions.test.ts
git commit -m "feat(u1c): the RPC bridge carries and parses the v3 shape"
```

---

### Task 6: Nơi phát sinh mutation — và vá lỗ tiến độ dở dang

**Files:**
- Modify: `src/components/app/state-provider.tsx:539-548` (`toggle`), `:565-609` (`setEntry`), `:631-646` + `:785-840` (mọi `upsertHabit`)
- Modify: `src/lib/sync/importer.ts:37-52` (habit), `:66-83` (log)
- Test: `src/lib/sync/importer.test.ts`, `src/components/app/state-provider.test.tsx`

**Interfaces:**
- Consumes: `SyncHabitPayload` v3, `SetHabitLogMutation.value/completedAt` (Task 2).
- Produces: helper xuất khẩu `habitSyncPayload(habit: DashboardHabit, sortOrder: number): SyncHabitPayload` trong `src/lib/sync/importer.ts` — dùng chung bởi importer **và** state-provider, để hai đường không lệch nhau.

**Lỗi phải vá ở bước này:** `setEntry` chỉ enqueue khi `after !== before` (số habit xong trong ngày). Uống 3 → 4 ly khi mục tiêu là 8 thì `after === before`, nên tiến độ dở dang **không bao giờ** rời máy. Điều kiện đúng là "ô log đã đổi".

- [ ] **Step 1: Viết test đỏ trong `src/lib/sync/importer.test.ts`**

```ts
describe("the initial upload carries the v3 shape (U1c)", () => {
  it("a habit uploads its full definition", () => {
    const day = "2026-07-27";
    const state = createHabitInState(createInitialDashboardState(day), {
      name: "Đọc sách",
      icon: "📚",
      trackingType: "duration",
      target: 20,
      unit: undefined,
      steps: [],
      repeatDays: [2, 4, 6],
      timesOfDay: ["evening"],
      scheduledAt: "21:00",
      color: "dusk",
      motivation: "Mỗi tối một chương"
    });

    const upload = buildInitialUpload(state, "fresh", day, "2026-07-27T10:00:00.000Z");
    const upsert = upload.find(
      (mutation) => mutation.kind === "upsertHabit" && mutation.habit.name === "Đọc sách"
    );

    expect(upsert).toBeTruthy();

    if (upsert?.kind !== "upsertHabit") throw new Error("expected an upsertHabit");

    expect(upsert.habit).toMatchObject({
      icon: "📚",
      trackingType: "duration",
      target: 20,
      repeatDays: [2, 4, 6],
      timesOfDay: ["evening"],
      scheduledAt: "21:00",
      color: "dusk",
      motivation: "Mỗi tối một chương"
    });
  });

  it("a completed cell uploads its reading and its clock", () => {
    const day = "2026-07-27";
    const base = createInitialDashboardState(day);
    const habitId = base.habits[0].id;
    const state = setHabitEntry(base, day, habitId, 1, "06:10");

    const upload = buildInitialUpload(state, "fresh", day, "2026-07-27T10:00:00.000Z");
    const log = upload.find(
      (mutation) => mutation.kind === "setHabitLog" && mutation.habitKey === habitId
    );

    if (log?.kind !== "setHabitLog") throw new Error("expected a setHabitLog");

    expect(log.value).toBe(1);
    expect(log.completedAt).toBe("06:10");
  });
});
```

- [ ] **Step 2: Viết test đỏ trong `src/components/app/state-provider.test.tsx`**

```ts
describe("partial progress reaches the cloud (U1c)", () => {
  it("adding a glass short of the target still enqueues a mutation", () => {
    // Before U1c the guard was "did the day's completed count change?", so
    // 3 -> 4 glasses of an 8-glass goal enqueued nothing and the reading never
    // left the device. The guard is now "did this cell change?".
    const enqueued: unknown[] = [];

    // markDirty is observed through the sync queue in localStorage — the
    // provider persists every mutation it mints.
    // (Use the file's existing renderWithProvider / queue-reading helper; if
    // none exists, read window.localStorage.getItem("betterme.syncqueue.v1").)
    expect(enqueued).toBeDefined();
  });
});
```

> **Thay thế bước này bằng test thật khi thực thi:** đọc phần đầu `state-provider.test.tsx` để dùng đúng helper render sẵn có, rồi kiểm chứng bằng cách đọc `betterme.syncqueue.v1` trong `localStorage` sau khi bấm `+1`. Nếu file test đó chưa dựng được engine sync (engine chỉ chạy khi có session), thì **bỏ test này** và thay bằng test thuần trong `src/lib/sync/importer.test.ts` cho `habitSyncPayload`, đồng thời tự tay kiểm chứng lỗ tiến độ trên trình duyệt ở Step 8. Ghi rõ lựa chọn vào PR.

- [ ] **Step 3: Chạy test, xác nhận đỏ**

```bash
npx vitest run src/lib/sync/importer.test.ts
```

Expected: FAIL — `upsert.habit` không có `icon`; `log.value` là `undefined`.

- [ ] **Step 4: Thêm `habitSyncPayload` vào `src/lib/sync/importer.ts` và dùng nó**

```ts
/**
 * The one place a DashboardHabit becomes a wire payload. The importer and the
 * live editor both go through it — two hand-written copies of this mapping is
 * exactly how a field ends up syncing on create but not on edit.
 */
export function habitSyncPayload(habit: DashboardHabit, sortOrder: number): SyncHabitPayload {
  return {
    key: habit.id,
    name: habit.name,
    category: habit.category,
    maxScore: habit.maxScore,
    active: habit.archivedAt == null,
    description: habit.description,
    sortOrder,
    icon: habit.icon,
    trackingType: habit.trackingType,
    target: habit.target,
    unit: habit.unit ?? null,
    steps: habit.steps ?? [],
    repeatDays: habit.repeatDays,
    timesOfDay: habit.timesOfDay,
    scheduledAt: habit.scheduledAt ?? null,
    color: habit.color,
    motivation: habit.motivation,
    pausedAt: habit.pausedAt ?? null,
    archivedAt: habit.archivedAt ?? null
  };
}
```

Sửa vòng habit trong `buildInitialUpload`:

```ts
  state.habits.filter((habit) => !tombstonedKeys.has(habit.id)).forEach((habit, index) => {
    mutations.push({
      kind: "upsertHabit",
      habit: habitSyncPayload(habit, index),
      clientTs: nowIso,
      expectCreate: true
    });
  });
```

Sửa vòng log — đọc `entries` (nguồn sự thật) thay vì chỉ `completions`:

```ts
  includedDates.forEach((date) => {
    const record = state.records[date];

    Object.keys(record.completions).forEach((habitKey) => {
      // Only real, currently-existing habits; only explicit ticks — a fresh
      // cloud garden reads absence as false, so false cells add nothing.
      if (!habitIds.has(habitKey)) return;
      if (record.completions[habitKey] !== true) return;

      const entry = record.entries[habitKey];
      const mutation: SetHabitLogMutation = {
        kind: "setHabitLog",
        habitKey,
        date,
        done: true,
        clientTs: nowIso
      };

      if (entry) {
        mutation.value = entry.value;
        if (entry.completedAt) mutation.completedAt = entry.completedAt;
      }

      mutations.push(mutation);
    });
  });
```

Thêm import:

```ts
import type { DashboardHabit } from "@/components/dashboard/dashboard-data";
import type { SetHabitLogMutation, SyncHabitPayload, SyncMutation } from "./types";
```

- [ ] **Step 5: Sửa `state-provider.tsx` — cả ba nơi mint mutation**

Trong `toggle` (khoảng dòng 539-548):

```ts
    if (habit) {
      // SET-based, idempotent (spec §2.1): the engine stamps the shadow cell.
      const entry = next.records[today]?.entries[habitId];

      markSyncDirty({
        kind: "setHabitLog",
        habitKey: habitId,
        date: today,
        done: turningOn,
        value: entry?.value ?? (turningOn ? 1 : 0),
        ...(entry?.completedAt ? { completedAt: entry.completedAt } : {}),
        clientTs: new Date().toISOString()
      });
    }
```

Trong `setEntry`, thay khối `if (after !== before) { markSyncDirty(...) }` bằng:

```ts
    // The guard is "did THIS cell change?", not "did the day's completed count
    // change?". 3 -> 4 glasses of an 8-glass goal leaves the count alone, and
    // under the old guard that reading never left the device.
    const entry = next.records[today]?.entries[habitId];

    markSyncDirty({
      kind: "setHabitLog",
      habitKey: habitId,
      date: today,
      done: next.records[today]?.completions[habitId] === true,
      value: entry?.value ?? 0,
      ...(entry?.completedAt ? { completedAt: entry.completedAt } : {}),
      clientTs: new Date().toISOString()
    });
```

(Bảo vệ "không có gì đổi thì không làm gì" đã nằm ở `if (after === before && next === base) return;` phía trên — đến đây thì chắc chắn ô log đã đổi.)

Trong `submitHabitDraft` (khoảng dòng 631-646) và hai chỗ `upsertHabit` còn lại (khoảng dòng 785-840 — pause/archive/reorder), thay khối `habit: { key: ..., sortOrder: ... }` bằng:

```ts
        habit: habitSyncPayload(habit, next.habits.findIndex((item) => item.id === habit.id)),
```

Thêm import:

```ts
import { habitSyncPayload } from "@/lib/sync/importer";
```

Xoá comment "The server contract is still v2-shaped (U1c widens it)" ở `submitHabitDraft` — nó đã hết đúng.

- [ ] **Step 6: Chạy toàn bộ test + typecheck**

```bash
npx vitest run && npx tsc --noEmit
```

Expected: PASS toàn bộ, `tsc` sạch.

- [ ] **Step 7: Chạy đủ 4 cổng**

```bash
npx tsc --noEmit && npx eslint . --max-warnings=0 && npx vitest run && npx next build
```

Expected: cả 4 xanh.

- [ ] **Step 8: Kiểm chứng bằng trình duyệt thật**

Chạy dev server, mở `/nep`, bấm `+1 ly` ba lần trên một habit đếm có mục tiêu 8, rồi đọc hàng đợi:

```bash
npm run dev
```

Trong console trình duyệt:

```js
JSON.parse(localStorage.getItem("betterme.syncqueue.v1"))
```

Expected: có đúng **một** mutation `setHabitLog` cho ô đó, `value: 3`, `done: false`. (Trước U1c: không có mutation nào.)

- [ ] **Step 9: Commit**

```bash
git add src/lib/sync/importer.ts src/lib/sync/importer.test.ts src/components/app/state-provider.tsx src/components/app/state-provider.test.tsx
git commit -m "fix(u1c): partial progress reaches the cloud, and habits upload their v3 shape"
```

---

### Task 7: Tài liệu — amendment spec + AGENTS + HANDOFF

**Files:**
- Modify: `docs/superpowers/specs/2026-07-08-social-garden-spec.md` (thêm ở **cuối file**)
- Modify: `AGENTS.md`, `HANDOFF.md`

**Interfaces:**
- Consumes: mọi quyết định của Task 1-6.
- Produces: không có code.

- [ ] **Step 1: Viết amendment ở cuối `docs/superpowers/specs/2026-07-08-social-garden-spec.md`**

```markdown
---

## Amendment 2026-07-27 — U1c: hợp đồng sync mang habit model v3

Spec gốc (§2.2-§2.4) mô tả một ô log là **một boolean**. Habit model v3 (spec
đại tu UI/UX §5) làm điều đó không còn đủ: một ô còn có *số đọc được* (mấy ly,
mấy phút, những bước nào đã tick) và *giờ hoàn thành*. Bản sửa đổi này mở rộng
hợp đồng theo hướng **thuần cộng thêm**.

**Cái không đổi.** `habit_logs.done` vẫn là boolean nguồn sự thật, do client
tính bằng `isEntryComplete` — chỉ client biết luật tracking của từng kiểu. Nhờ
vậy `refresh_my_summary` (§5.2, `weekly_good_days`) và vòng lặp
`shared_rhythms` (§5.1) đọc `done` không phải sửa gì. Đây là lựa chọn có ý
thức: server không suy diễn "xong" từ `value` vì server không biết mục tiêu.

**Cái thêm vào.**

| Bảng | Cột | Ghi chú |
|---|---|---|
| `habit_logs` | `value integer` | NULL = ghi bởi client trước U1c hoặc schema cũ |
| `habit_logs` | `completed_at text` | "HH:mm" giờ **địa phương**, ngày đã nằm ở `date` |
| `habits` | `icon`, `tracking_type`, `target`, `unit`, `steps`, `repeat_days`, `times_of_day`, `scheduled_at`, `color`, `motivation`, `paused_at`, `archived_at` | định nghĩa v3 (§5.1 spec đại tu) |

**LWW không đổi độ mịn.** Vẫn **một stamp cho cả ô** (`mutated_at`) và **một
stamp cho cả habit** (`client_updated_at`). Bên nào thắng thì mang trọn cả cụm
field sang — không có LWW theo từng cột.

**Hai chiều đều chịu được đối phương ở schema cũ.**
- *App mới, DB cũ*: RPC thiếu tham số → PostgREST trả `PGRST202`; client xếp
  loại **retry** (không phải permanent), nên hàng đợi tự đẩy lại ngay khi chủ
  repo apply `schema.sql`. Không mất ghi.
- *DB cũ trả snapshot thiếu cột*: `value = null` ⇒ merge giữ đúng hành vi
  trước U1c (tick từ xa giữ lại giá trị giàu hơn ở máy này, không dập "8 ly"
  thành 1). Habit thiếu field v3 ⇒ parse ra đúng default mà
  `migrateHabitFields` vốn sẽ áp.

**Bẫy chữ ký hàm.** `create or replace function` với danh sách tham số khác sẽ
tạo **overload**, không thay thế. Hai overload cùng tên + gọi bằng named
argument (PostgREST luôn gọi thế) = `42725 function is not unique`. Nên
`apply_habit_log` và `upsert_habit` đều có `drop function if exists <chữ ký
cũ>` đứng trước. Drop cũng xoá grant cũ ⇒ chữ ký mới phải được
`revoke ... from public, anon` + `grant ... to authenticated` lại, nếu không
hàm mới thừa hưởng mặc định `EXECUTE` cho `PUBLIC`. `tests/schema-contract.test.ts`
canh cả hai điều này vì CI không có DB để chạy SQL.

**Thứ tự triển khai (bắt buộc).** Apply `supabase/schema.sql` lên Supabase
**trước**, deploy app sau. Làm ngược lại thì app vẫn chạy và không mất dữ liệu
(nhờ PGRST202-retry ở trên), nhưng sync đứng im cho tới khi SQL được apply.

**Vẫn không bao giờ rời máy:** `seedCutoverDate`, `bestStreakFloor`, `events` —
chúng không có mutation kind nào (§2.5).
```

- [ ] **Step 2: Cập nhật `AGENTS.md`**

Trong mục quy ước sync, thêm:

```markdown
- **Hợp đồng sync là v3 từ U1c.** Một ô log mang `value` + `completedAt` bên
  cạnh `done`; `done` vẫn do client tính (`isEntryComplete`) và vẫn là thứ
  `refresh_my_summary` / `shared_rhythms` đọc. Một habit mang đủ 12 field v3.
- **Một `DashboardHabit` thành payload chỉ qua `habitSyncPayload()`**
  (`src/lib/sync/importer.ts`). Đừng viết tay mapping lần thứ hai — đó là cách
  một field sync lúc tạo mới nhưng im lặng lúc sửa.
- **Đổi chữ ký một RPC ⇒ `drop function if exists` chữ ký cũ + grant lại chữ
  ký mới.** `tests/schema-contract.test.ts` canh chỗ này; nó đọc `schema.sql`
  như văn bản vì CI không có DB.
- **Enqueue log theo "ô này có đổi không", không phải "số habit xong có đổi
  không".** Cái sau bỏ rơi mọi tiến độ dở dang.
```

Cập nhật dòng "Current state: N tests green" theo số thật sau khi chạy.

- [ ] **Step 3: Cập nhật `HANDOFF.md`**

- Thêm dòng U1c vào bảng "đã ship": nhánh, số commit, số test, một câu mô tả.
- Ghi rõ **việc của chủ repo**: apply `supabase/schema.sql` lên Supabase trước khi deploy; SQL idempotent nên chạy lại an toàn.
- Cập nhật số test và tên nhánh hiện tại.
- Chuyển U1c khỏi mục "còn treo"; bước kế tiếp là **U2** (hero bầu trời, tab Ngày/Tuần, lưới tuần, chuyển analytics sang `/calendar`).

- [ ] **Step 4: Chạy đủ 4 cổng lần cuối**

```bash
npx tsc --noEmit && npx eslint . --max-warnings=0 && npx vitest run && npx next build
```

Expected: cả 4 xanh.

- [ ] **Step 5: Commit**

```bash
git add docs/superpowers/specs/2026-07-08-social-garden-spec.md AGENTS.md HANDOFF.md
git commit -m "docs: U1c shipped — the sync contract speaks habit v3"
```

- [ ] **Step 6: Hoàn tất nhánh**

**REQUIRED SUB-SKILL:** `superpowers:finishing-a-development-branch`.

---

## Self-Review

**1. Spec coverage.** Ba việc `HANDOFF.md` liệt kê cho U1c:
- "cột mới trong `supabase/schema.sql`" → Task 1.
- "soát `merge.ts`/`importer.ts` từng field" → Task 3 (log), Task 4 (habit, chiều xuống), Task 6 (importer + state-provider, chiều lên). `queue.ts` được soát ở Task 2, `engine.ts` không cần đổi (nó chỉ chuyển tiếp mutation và đóng dấu shadow theo `clientTs` — không đọc field nào của payload).
- "ghi Amendment vào cuối spec social" → Task 7.

**2. Placeholder scan.** Một chỗ cố ý để ngỏ: Task 6 Step 2 (test state-provider) phụ thuộc vào helper sẵn có trong `state-provider.test.tsx` mà kế hoạch này chưa đọc. Đã ghi rõ đường lui và yêu cầu ghi lựa chọn vào PR, kèm bước kiểm chứng trình duyệt thật ở Step 8 để lỗ tiến độ dở dang không thể đi lọt mà không ai nhìn thấy.

**3. Type consistency.** `SyncHabitPayload` và `ServerHabit` dùng chung đúng 12 tên field v3 (Task 2) — `habitSyncPayload` (Task 6) sản xuất chúng, `serverV3Fields` (Task 4) tiêu thụ chúng, `parseHabit` (Task 5) dựng chúng. `ServerHabitLog.value: number | null` (Task 2) khớp `serverEntry` (Task 3) và `nullableNumber` (Task 5). `SetHabitLogMutation.value?: number` là optional (client), còn `ServerHabitLog.value` là nullable (server) — cố ý khác nhau: client "chưa có gì để gửi", server "biết chắc là không biết".

**4. Rủi ro còn lại, nói thẳng.** Không có DB trong CI, nên **không một dòng SQL nào ở Task 1 được chạy thật** trong quá trình này. `tests/schema-contract.test.ts` chỉ canh được lỗi chữ ký và cột thiếu ở mức văn bản. Việc apply lên Supabase là của chủ repo, và lần apply đầu tiên là lần đầu SQL này thực sự chạy.
