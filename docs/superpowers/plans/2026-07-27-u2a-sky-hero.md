# U2a — Hero "bầu trời" + hai primitive còn nợ Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thay hero cũ (còn nói ngôn ngữ thị giác v2: `matcha`, `plum`, `wafer`) bằng **hero bầu trời** đổi theo buổi trong ngày, kèm hai primitive `ProgressRing` + `TabSwitch` nợ từ U0.

**Architecture:** Bầu trời là **ba bộ token**, không phải ba class rời — mỗi buổi có `from`/`to`/`ink`/`ink-soft`, và gate tương phản kiểm text trên **cả hai đầu gradient** vì bầu trời tối lật chữ sang sáng. `ProgressRing` gom cái vòng conic đang viết tay trong `habit-entry-control.tsx`. Thời tiết chuyển vào `StateProvider` để hero và thẻ thời tiết đọc **một** lần fetch chứ không phải hai.

**Tech Stack:** Next.js 15 App Router, React 19, TS strict, Tailwind 3.4 + CSS-variable token, Vitest + Testing Library.

## Global Constraints

- **Màu là vai trò, không phải trang trí** (spec §2.1). `--action` là màu primary/streak/link DUY NHẤT; `--success` là hoàn thành; `--alert` chỉ dành cho badge tin mới.
- **Tương phản là gate, không phải ý kiến**: text ≥ 4.5:1, ranh giới control ≥ 3:1 (WCAG 1.4.11). `src/app/design-tokens.test.ts` enforce.
- **Tailwind + `var()`**: modifier opacity (`bg-sky-morning/10`) **không chạy** với màu giá trị `var()`. Cần độ mờ thì thêm token, đừng thêm modifier.
- **No-guilt** (invariant tuyệt đối): không câu chữ nào trách móc hay so sánh user *xuống dưới* ai. Câu của Nếp phải đi qua voice pack đã có guard.
- **Chạm tối thiểu 44×44px**; mọi control có tên khả truy cập tiếng Việt.
- Comment code bằng tiếng Anh, giải thích *tại sao*. Chuỗi hiển thị tiếng Việt.
- Motion tôn trọng `prefers-reduced-motion`.

---

## File Structure

| File | Trách nhiệm | Việc của U2a |
|---|---|---|
| `src/app/globals.css` | Token gốc | **Tạo** 12 token bầu trời (3 buổi × from/to/ink/ink-soft) |
| `tailwind.config.ts` | Map token → utility | Map cả 12 |
| `src/app/design-tokens.test.ts` | Gate tương phản | Kiểm ink của mỗi buổi trên **cả hai** đầu gradient |
| `src/components/dashboard/sky.ts` | **Tạo mới** — logic bầu trời thuần | `skyPhaseAt(hour)`, `SKY_STYLES`, `SKY_LABELS` |
| `src/components/ui/progress-ring.tsx` | **Tạo mới** | Vòng tiến độ dùng chung |
| `src/components/ui/tab-switch.tsx` | **Tạo mới** | Tab Ngày/Tuần (U2b nối vào) |
| `src/components/dashboard/habit-entry-control.tsx` | Điều khiển ghi nhận | Bỏ vòng conic viết tay, dùng `ProgressRing` |
| `src/components/app/state-provider.tsx` | Toàn bộ state app | Sở hữu thời tiết: một fetch, một nguồn |
| `src/components/dashboard/weather-card.tsx` | Thẻ thời tiết | Thành **consumer**, không tự fetch nữa |
| `src/components/dashboard/hero-banner.tsx` | Hero | Viết lại theo §4.1 |

---

## Bối cảnh: ba chỗ dễ sai

**(1) Bầu trời tối lật chữ sang sáng.** Sáng/chiều là nền ấm → chữ đậm. Tối là tím than → chữ phải sáng. Nếu chỉ đổi gradient mà giữ `--ink`, chữ tối trên nền tối là lỗi a11y hạng nặng và **không test nào hiện có bắt được** — nên gate phải kiểm từng buổi.

**(2) Gradient có HAI đầu.** Chữ nằm trên cả dải, nên `ink` phải đạt 4.5:1 với **cả** `from` **và** `to`. Kiểm mỗi một đầu là tự lừa mình.

**(3) Hai lần fetch thời tiết.** Hero (§4.1) và thẻ thời tiết cùng cần một snapshot. Để mỗi bên tự `fetchWeather` là gọi mạng hai lần và hai bên có thể hiện hai con số khác nhau. Thời tiết chuyển vào `StateProvider` — đúng quy ước đã ghi trong `AGENTS.md` ("`state-provider.tsx` — ALL app state").

---

### Task 1: Token bầu trời + gate tương phản

**Files:**
- Modify: `src/app/globals.css` (khối `:root`), `tailwind.config.ts`, `src/app/design-tokens.test.ts`

**Interfaces:**
- Produces: token CSS `--sky-{morning,afternoon,evening}-{from,to,ink,ink-soft}`; utility Tailwind `bg-sky-morning-from`, `text-sky-evening-ink`, …

- [ ] **Step 1: Viết test đỏ trong `src/app/design-tokens.test.ts`**

Thêm sau khối `CONTROL_PAIRS`:

```ts
/**
 * The sky gradient (spec §4.1). Text sits across the WHOLE band, so each
 * phase's ink is checked against BOTH gradient stops — passing on one end
 * only is self-deception. The evening sky is dark, so its ink flips light;
 * nothing but this gate would have caught dark-on-dark.
 */
const SKY_PHASES = ["morning", "afternoon", "evening"] as const;

describe("sky gradient legibility (spec §4.1)", () => {
  SKY_PHASES.forEach((phase) => {
    (["ink", "ink-soft"] as const).forEach((role) => {
      (["from", "to"] as const).forEach((stop) => {
        it(`--sky-${phase}-${role} is readable on --sky-${phase}-${stop}`, () => {
          const tokens = readTokens(css);
          const ratio = contrastRatio(
            tokens[`sky-${phase}-${role}`],
            tokens[`sky-${phase}-${stop}`]
          );

          expect(ratio, `sky-${phase}-${role} on ${stop} = ${ratio.toFixed(2)}:1`).toBeGreaterThanOrEqual(4.5);
        });
      });
    });
  });
});
```

- [ ] **Step 2: Chạy test, xác nhận đỏ**

```bash
npx vitest run src/app/design-tokens.test.ts
```

Expected: FAIL — 12 test đỏ, token chưa tồn tại (`contrastRatio(undefined, …)` → NaN).

- [ ] **Step 3: Thêm token vào `src/app/globals.css`**

Chèn sau khối màu habit (`--habit-sand-strong`):

```css
    /* ——— Bầu trời (spec §4.1). Ba buổi, mỗi buổi một cặp gradient + bộ ink
       riêng. Buổi tối là nền TỐI nên ink lật sang sáng — đó là lý do ink
       thuộc về từng buổi chứ không dùng chung --ink. Gate tương phản kiểm
       ink trên CẢ HAI đầu gradient (src/app/design-tokens.test.ts). ——— */
    --sky-morning-from: #FFF4D6;
    --sky-morning-to: #FBD9A0;
    --sky-morning-ink: #3B2A12;
    --sky-morning-ink-soft: #6B4E1F;

    --sky-afternoon-from: #FFE3C7;
    --sky-afternoon-to: #F8BE9A;
    --sky-afternoon-ink: #40200E;
    --sky-afternoon-ink-soft: #6E3A1C;

    --sky-evening-from: #2E2A4A;
    --sky-evening-to: #4A3F6B;
    --sky-evening-ink: #F6F2FF;
    --sky-evening-ink-soft: #D8D0F0;
```

- [ ] **Step 4: Map trong `tailwind.config.ts`**

Thêm vào `colors` (cạnh khối `habit`):

```ts
        sky: {
          "morning-from": "var(--sky-morning-from)",
          "morning-to": "var(--sky-morning-to)",
          "morning-ink": "var(--sky-morning-ink)",
          "morning-ink-soft": "var(--sky-morning-ink-soft)",
          "afternoon-from": "var(--sky-afternoon-from)",
          "afternoon-to": "var(--sky-afternoon-to)",
          "afternoon-ink": "var(--sky-afternoon-ink)",
          "afternoon-ink-soft": "var(--sky-afternoon-ink-soft)",
          "evening-from": "var(--sky-evening-from)",
          "evening-to": "var(--sky-evening-to)",
          "evening-ink": "var(--sky-evening-ink)",
          "evening-ink-soft": "var(--sky-evening-ink-soft)"
        },
```

> ⚠️ Tailwind đã có palette `sky` mặc định. Khai báo `sky` trong `theme.extend.colors` sẽ **ghi đè** nó. Kiểm bằng `rg "sky-[0-9]" src` — nếu có chỗ dùng `sky-500` thì đổi tên khoá thành `skyband`. Ghi kết quả kiểm vào PR.

- [ ] **Step 5: Chạy test, xác nhận xanh**

```bash
npx vitest run src/app/design-tokens.test.ts
```

Expected: PASS. Nếu một cặp trượt, **đổi màu cho đạt** — đừng hạ ngưỡng.

- [ ] **Step 6: Commit**

```bash
git add src/app/globals.css tailwind.config.ts src/app/design-tokens.test.ts
git commit -m "feat(u2a): the sky has three phases, and every one of them is legible"
```

---

### Task 2: `sky.ts` — buổi nào thì bầu trời nào

**Files:**
- Create: `src/components/dashboard/sky.ts`, `src/components/dashboard/sky.test.ts`

**Interfaces:**
- Produces:
  - `type SkyPhase = "morning" | "afternoon" | "evening"`
  - `skyPhaseAt(hour: number): SkyPhase`
  - `SKY_STYLES: Record<SkyPhase, { panel: string; ink: string; inkSoft: string; hairline: string }>`
  - `SKY_GREETINGS: Record<SkyPhase, string>`

- [ ] **Step 1: Viết test đỏ — `src/components/dashboard/sky.test.ts`**

```ts
import { describe, expect, it } from "vitest";

import { SKY_GREETINGS, SKY_STYLES, skyPhaseAt } from "./sky";

describe("skyPhaseAt", () => {
  it("splits the day the same way habit buổi does", () => {
    // Deliberately the same boundaries as TimeOfDay in habit-model: a habit
    // filed under 🌙 Tối and the evening sky must not disagree about when
    // evening starts.
    expect(skyPhaseAt(5)).toBe("morning");
    expect(skyPhaseAt(11)).toBe("morning");
    expect(skyPhaseAt(12)).toBe("afternoon");
    expect(skyPhaseAt(17)).toBe("afternoon");
    expect(skyPhaseAt(18)).toBe("evening");
    expect(skyPhaseAt(23)).toBe("evening");
  });

  it("the small hours are still last night, not tomorrow morning", () => {
    // 02:00 is someone still awake, not someone up early.
    expect(skyPhaseAt(0)).toBe("evening");
    expect(skyPhaseAt(4)).toBe("evening");
  });

  it("survives a junk hour instead of rendering an undefined sky", () => {
    expect(skyPhaseAt(-1)).toBe("evening");
    expect(skyPhaseAt(99)).toBe("evening");
    expect(skyPhaseAt(Number.NaN)).toBe("evening");
  });
});

describe("SKY_STYLES", () => {
  it("names a class set for every phase", () => {
    (["morning", "afternoon", "evening"] as const).forEach((phase) => {
      expect(SKY_STYLES[phase].panel).toContain(`sky-${phase}-from`);
      expect(SKY_STYLES[phase].panel).toContain(`sky-${phase}-to`);
      expect(SKY_STYLES[phase].ink).toBe(`text-sky-${phase}-ink`);
      expect(SKY_STYLES[phase].inkSoft).toBe(`text-sky-${phase}-ink-soft`);
    });
  });

  it("greets in Vietnamese, one per phase", () => {
    expect(SKY_GREETINGS.morning).toBe("Chào buổi sáng");
    expect(SKY_GREETINGS.afternoon).toBe("Chào buổi chiều");
    expect(SKY_GREETINGS.evening).toBe("Chào buổi tối");
  });
});
```

- [ ] **Step 2: Chạy test, xác nhận đỏ**

```bash
npx vitest run src/components/dashboard/sky.test.ts
```

Expected: FAIL — `Cannot find module './sky'`.

- [ ] **Step 3: Viết `src/components/dashboard/sky.ts`**

```ts
/**
 * The hero's sky (spec §4.1) — pure, no React, no clock of its own. The hour
 * is handed in so a test can put the sun anywhere it likes.
 */

export type SkyPhase = "morning" | "afternoon" | "evening";

/**
 * Boundaries match TimeOfDay in habit-model on purpose: a habit filed under
 * 🌙 Tối and the evening sky must not disagree about when evening starts.
 * Anything outside 05:00–17:59 — including the small hours and any junk
 * number — is evening, because 02:00 is someone still awake rather than
 * someone up early.
 */
export function skyPhaseAt(hour: number): SkyPhase {
  if (!Number.isFinite(hour)) return "evening";
  if (hour >= 5 && hour < 12) return "morning";
  if (hour >= 12 && hour < 18) return "afternoon";

  return "evening";
}

/**
 * Tailwind class sets per phase. Written out in full rather than built by
 * template string — Tailwind scans source text, and a class it cannot see
 * literally is a class it does not generate.
 */
export const SKY_STYLES: Record<
  SkyPhase,
  { panel: string; ink: string; inkSoft: string; hairline: string }
> = {
  morning: {
    panel: "bg-gradient-to-br from-sky-morning-from to-sky-morning-to",
    ink: "text-sky-morning-ink",
    inkSoft: "text-sky-morning-ink-soft",
    hairline: "border-sky-morning-to"
  },
  afternoon: {
    panel: "bg-gradient-to-br from-sky-afternoon-from to-sky-afternoon-to",
    ink: "text-sky-afternoon-ink",
    inkSoft: "text-sky-afternoon-ink-soft",
    hairline: "border-sky-afternoon-to"
  },
  evening: {
    panel: "bg-gradient-to-br from-sky-evening-from to-sky-evening-to",
    ink: "text-sky-evening-ink",
    inkSoft: "text-sky-evening-ink-soft",
    hairline: "border-sky-evening-to"
  }
};

export const SKY_GREETINGS: Record<SkyPhase, string> = {
  morning: "Chào buổi sáng",
  afternoon: "Chào buổi chiều",
  evening: "Chào buổi tối"
};
```

- [ ] **Step 4: Chạy test, xác nhận xanh**

```bash
npx vitest run src/components/dashboard/sky.test.ts
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/components/dashboard/sky.ts src/components/dashboard/sky.test.ts
git commit -m "feat(u2a): the sky knows what time of day it is"
```

---

### Task 3: `ProgressRing` — một vòng tiến độ, không phải ba

**Files:**
- Create: `src/components/ui/progress-ring.tsx`, `src/components/ui/progress-ring.test.tsx`
- Modify: `src/components/dashboard/habit-entry-control.tsx` (bỏ `ProgressBadge` viết tay)

**Interfaces:**
- Produces: `ProgressRing({ value, target, size, tone, label, children })` — `size: "sm" | "md" | "lg"`, `tone: "action" | "success"`.

- [ ] **Step 1: Viết test đỏ — `src/components/ui/progress-ring.test.tsx`**

```ts
import { render, screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";

import { ProgressRing } from "./progress-ring";

describe("ProgressRing", () => {
  it("reports progress to assistive tech, not just to the eye", () => {
    render(<ProgressRing label="Tiến độ hôm nay" target={4} value={2} />);

    const ring = screen.getByRole("progressbar", { name: "Tiến độ hôm nay" });

    expect(ring.getAttribute("aria-valuenow")).toBe("2");
    expect(ring.getAttribute("aria-valuemax")).toBe("4");
  });

  it("never renders a ratio outside 0–100%", () => {
    // A count habit can overshoot its target (10 glasses of an 8-glass goal)
    // and a corrupt cell can go negative; neither may produce a broken ring.
    const { container: over } = render(<ProgressRing label="a" target={8} value={99} />);
    const { container: under } = render(<ProgressRing label="b" target={8} value={-5} />);

    expect(over.querySelector("[data-ratio]")?.getAttribute("data-ratio")).toBe("100");
    expect(under.querySelector("[data-ratio]")?.getAttribute("data-ratio")).toBe("0");
  });

  it("a zero target is a full ring, not a division by zero", () => {
    const { container } = render(<ProgressRing label="a" target={0} value={0} />);

    expect(container.querySelector("[data-ratio]")?.getAttribute("data-ratio")).toBe("100");
  });

  it("renders whatever it is given in the middle", () => {
    render(
      <ProgressRing label="Tiến độ" target={4} value={2}>
        <span>2/4</span>
      </ProgressRing>
    );

    expect(screen.getByText("2/4")).toBeTruthy();
  });
});
```

- [ ] **Step 2: Chạy test, xác nhận đỏ**

```bash
npx vitest run src/components/ui/progress-ring.test.tsx
```

Expected: FAIL — `Cannot find module './progress-ring'`.

- [ ] **Step 3: Viết `src/components/ui/progress-ring.tsx`**

```tsx
import { cn } from "@/lib/utils";

const SIZES = {
  sm: "h-9 w-9 text-[10px]",
  md: "h-12 w-12 text-xs",
  lg: "h-16 w-16 text-sm"
} as const;

/**
 * A ring that fills as progress is made. The fill is a conic-gradient masked
 * by a radial one — no SVG, no library, and it inherits the token colours.
 *
 * `target <= 0` renders full rather than dividing by zero: a habit with
 * nothing to reach has nothing left to do.
 */
export function ProgressRing({
  children,
  className,
  label,
  size = "sm",
  target,
  tone = "action",
  value
}: {
  children?: React.ReactNode;
  className?: string;
  /** Accessible name — required; a bare ring tells a screen reader nothing. */
  label: string;
  size?: keyof typeof SIZES;
  target: number;
  tone?: "action" | "success";
  value: number;
}) {
  const ratio = target <= 0 ? 1 : Math.min(1, Math.max(0, value / target));
  const percent = Math.round(ratio * 100);

  return (
    <span
      aria-label={label}
      aria-valuemax={target}
      aria-valuemin={0}
      aria-valuenow={value}
      className={cn(
        "flex shrink-0 items-center justify-center rounded-full font-bold",
        SIZES[size],
        className
      )}
      data-ratio={percent}
      role="progressbar"
      style={{
        background: `radial-gradient(circle at center, var(--surface-card) 68%, transparent 69%), conic-gradient(var(--${tone}) ${percent}%, var(--line-strong) 0)`
      }}
    >
      {children}
    </span>
  );
}
```

- [ ] **Step 4: Chạy test, xác nhận xanh**

```bash
npx vitest run src/components/ui/progress-ring.test.tsx
```

Expected: PASS.

- [ ] **Step 5: Thay vòng viết tay trong `habit-entry-control.tsx`**

Xoá hàm `ProgressBadge` ở cuối file và thay bằng:

```tsx
/** A ring that fills as the day progresses; solid green once the target is met. */
function ProgressBadge({
  done,
  label,
  progress
}: {
  done: boolean;
  label: string;
  progress: { done: number; target: number };
}) {
  if (done) {
    return (
      <span className="check-pop flex h-9 w-9 items-center justify-center rounded-full bg-success text-white">
        <Check aria-hidden="true" className="h-4 w-4" strokeWidth={3.5} />
        <span className="sr-only">{label}</span>
      </span>
    );
  }

  return (
    <ProgressRing className="text-ink-soft" label={label} target={progress.target} value={progress.done}>
      {label}
    </ProgressRing>
  );
}
```

Sửa hai call site trong file: `<ProgressBadge done={done} label={...} ratio={progress.ratio} />` → `<ProgressBadge done={done} label={`${progress.done}/${progress.target}`} progress={progress} />`. Thêm `import { ProgressRing } from "@/components/ui/progress-ring";`.

- [ ] **Step 6: Chạy lại test của điều khiển ghi nhận**

```bash
npx vitest run src/components/dashboard/habit-entry-control.test.tsx
```

Expected: PASS — bộ test cũ là lưới an toàn cho lần thay này.

> Nếu một test cũ đỏ vì tìm text `"3/8"` mà nay nó nằm trong `ProgressRing`: đó là **cùng một text ở cùng một chỗ**, chỉ đổi phần tử bao ngoài. Sửa selector, đừng sửa kỳ vọng.

- [ ] **Step 7: Commit**

```bash
git add src/components/ui/progress-ring.tsx src/components/ui/progress-ring.test.tsx src/components/dashboard/habit-entry-control.tsx
git commit -m "feat(u2a): one progress ring, used everywhere a ring is needed"
```

---

### Task 4: `TabSwitch` — Ngày / Tuần

**Files:**
- Create: `src/components/ui/tab-switch.tsx`, `src/components/ui/tab-switch.test.tsx`

**Interfaces:**
- Produces: `TabSwitch<T extends string>({ label, onChange, options, value })` với `options: Array<{ value: T; label: string }>`.

U2b nối vào `today-page.tsx`; U2a chỉ dựng primitive + test.

- [ ] **Step 1: Viết test đỏ — `src/components/ui/tab-switch.test.tsx`**

```tsx
import { fireEvent, render, screen } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";

import { TabSwitch } from "./tab-switch";

const OPTIONS = [
  { value: "day", label: "Hôm nay" },
  { value: "week", label: "Tuần này" }
];

describe("TabSwitch", () => {
  it("is a real tablist, so a screen reader announces which view is showing", () => {
    render(<TabSwitch label="Chế độ xem" onChange={vi.fn()} options={OPTIONS} value="day" />);

    expect(screen.getByRole("tablist", { name: "Chế độ xem" })).toBeTruthy();
    expect(screen.getByRole("tab", { name: "Hôm nay" }).getAttribute("aria-selected")).toBe("true");
    expect(screen.getByRole("tab", { name: "Tuần này" }).getAttribute("aria-selected")).toBe("false");
  });

  it("reports the value picked", () => {
    const onChange = vi.fn();

    render(<TabSwitch label="Chế độ xem" onChange={onChange} options={OPTIONS} value="day" />);
    fireEvent.click(screen.getByRole("tab", { name: "Tuần này" }));

    expect(onChange).toHaveBeenCalledWith("week");
  });

  it("moves with the arrow keys and wraps around", () => {
    // A tablist that only responds to clicks is unreachable from a keyboard —
    // arrow keys are how the pattern is actually navigated.
    const onChange = vi.fn();

    render(<TabSwitch label="Chế độ xem" onChange={onChange} options={OPTIONS} value="day" />);

    const first = screen.getByRole("tab", { name: "Hôm nay" });

    fireEvent.keyDown(first, { key: "ArrowRight" });
    expect(onChange).toHaveBeenCalledWith("week");

    fireEvent.keyDown(first, { key: "ArrowLeft" });
    expect(onChange).toHaveBeenLastCalledWith("week"); // wraps to the last one
  });

  it("keeps only the selected tab in the tab order (roving tabindex)", () => {
    render(<TabSwitch label="Chế độ xem" onChange={vi.fn()} options={OPTIONS} value="week" />);

    expect(screen.getByRole("tab", { name: "Hôm nay" }).getAttribute("tabindex")).toBe("-1");
    expect(screen.getByRole("tab", { name: "Tuần này" }).getAttribute("tabindex")).toBe("0");
  });
});
```

- [ ] **Step 2: Chạy test, xác nhận đỏ**

```bash
npx vitest run src/components/ui/tab-switch.test.tsx
```

Expected: FAIL — `Cannot find module './tab-switch'`.

- [ ] **Step 3: Viết `src/components/ui/tab-switch.tsx`**

```tsx
"use client";

import { cn } from "@/lib/utils";

/**
 * Two or three views, one visible at a time (spec §4.2). Implements the ARIA
 * tabs pattern properly: arrow keys move between tabs and only the selected
 * one stays in the tab order, so Tab steps past the whole group rather than
 * through every option.
 *
 * The panel itself is the caller's business — this switch owns no content.
 */
export function TabSwitch<T extends string>({
  className,
  label,
  onChange,
  options,
  value
}: {
  className?: string;
  /** Accessible name for the group, e.g. "Chế độ xem". */
  label: string;
  onChange: (value: T) => void;
  options: Array<{ value: T; label: string }>;
  value: T;
}) {
  const index = options.findIndex((option) => option.value === value);

  function move(delta: number) {
    const next = (index + delta + options.length) % options.length;

    onChange(options[next].value);
  }

  return (
    <div
      aria-label={label}
      className={cn(
        "inline-flex items-center gap-1 rounded-pill border border-line bg-surface-card p-1",
        className
      )}
      role="tablist"
    >
      {options.map((option) => {
        const selected = option.value === value;

        return (
          <button
            aria-selected={selected}
            className={cn(
              "squishy min-h-[44px] rounded-pill px-4 text-sm font-semibold transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-action focus-visible:ring-offset-2 focus-visible:ring-offset-surface-page",
              selected
                ? "bg-action text-action-ink shadow-action"
                : "text-ink-mid hover:bg-surface-warm"
            )}
            key={option.value}
            onClick={() => onChange(option.value)}
            onKeyDown={(event) => {
              if (event.key === "ArrowRight" || event.key === "ArrowDown") {
                event.preventDefault();
                move(1);
              }

              if (event.key === "ArrowLeft" || event.key === "ArrowUp") {
                event.preventDefault();
                move(-1);
              }
            }}
            role="tab"
            tabIndex={selected ? 0 : -1}
            type="button"
          >
            {option.label}
          </button>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 4: Chạy test, xác nhận xanh**

```bash
npx vitest run src/components/ui/tab-switch.test.tsx
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/components/ui/tab-switch.tsx src/components/ui/tab-switch.test.tsx
git commit -m "feat(u2a): a keyboard-navigable tab switch, ready for Ngày/Tuần"
```

---

### Task 5: Thời tiết về `StateProvider` — một fetch, một nguồn

**Files:**
- Modify: `src/components/app/state-provider.tsx`, `src/components/dashboard/weather-card.tsx`
- Test: `src/components/dashboard/weather-card.test.tsx`

**Interfaces:**
- Produces trên `AppState`:
  - `weather: { status: "loading" | "ready" | "error"; snapshot: WeatherSnapshot | null; place: WeatherPlace }`
  - `setWeatherPlace: (place: WeatherPlace) => void`

Hero (Task 6) và `WeatherCard` cùng đọc `app.weather`. Không bên nào tự `fetchWeather` nữa.

- [ ] **Step 1: Đọc `weather-card.tsx` và liệt kê state nó đang giữ**

```bash
npx rg -n "useState|useEffect|fetchWeather|loadWidgetSettings|saveWidgetSettings" src/components/dashboard/weather-card.tsx
```

Ghi lại: cái nào là **dữ liệu** (đi lên provider) và cái nào là **UI cục bộ** (ô tìm thành phố đang mở hay đóng — ở lại thẻ).

- [ ] **Step 2: Viết test đỏ trong `src/components/dashboard/weather-card.test.tsx`**

Thêm:

```tsx
describe("weather has ONE source (U2a)", () => {
  it("the card renders the provider's snapshot without fetching again", async () => {
    // Two independent fetches would be two network calls for one number, and
    // the hero and the card could disagree about the temperature.
    const fetchSpy = vi.fn(async () => {
      throw new Error("the card must not fetch");
    });

    vi.stubGlobal("fetch", fetchSpy);

    render(
      <StateProvider userEmail="thien@example.com">
        <WeatherCard />
      </StateProvider>
    );

    // The provider owns the single call; the card never adds one of its own.
    expect(fetchSpy.mock.calls.length).toBeLessThanOrEqual(1);
  });
});
```

> Đọc phần đầu file test hiện có trước — nó đã có cách stub `fetch` riêng. Dùng lại cách đó thay vì dựng cách thứ hai.

- [ ] **Step 3: Chuyển state thời tiết lên `state-provider.tsx`**

Thêm cạnh các `useState` khác:

```tsx
  const [weatherPlace, setWeatherPlaceState] = useState<WeatherPlace>(DEFAULT_WEATHER_PLACE);
  const [weatherSnapshot, setWeatherSnapshot] = useState<WeatherSnapshot | null>(null);
  const [weatherStatus, setWeatherStatus] = useState<"loading" | "ready" | "error">("loading");
```

Và một effect:

```tsx
  // ONE fetch for the whole app: the hero's weather line (spec §4.1) and the
  // weather card show the same number because they read the same state. Two
  // components fetching independently would be two calls that can disagree.
  useEffect(() => {
    setWeatherPlaceState(loadWidgetSettings().weatherPlace);
  }, []);

  useEffect(() => {
    let cancelled = false;

    setWeatherStatus("loading");

    void fetchWeather(weatherPlace)
      .then((snapshot) => {
        if (cancelled) return;

        setWeatherSnapshot(snapshot);
        setWeatherStatus(snapshot ? "ready" : "error");
      })
      .catch(() => {
        if (!cancelled) setWeatherStatus("error");
      });

    return () => {
      cancelled = true;
    };
  }, [weatherPlace]);

  const setWeatherPlace = useCallback((place: WeatherPlace) => {
    saveWidgetSettings({ weatherPlace: place });
    setWeatherPlaceState(place);
  }, []);
```

Thêm vào `AppState` type và vào object `value`:

```tsx
  weather: { status: "loading" | "ready" | "error"; snapshot: WeatherSnapshot | null; place: WeatherPlace };
  setWeatherPlace: (place: WeatherPlace) => void;
```

> `saveWidgetSettings` hiện nhận shape gì thì gọi đúng shape đó — đọc `widget-settings.ts` trước khi viết, đừng đoán.

- [ ] **Step 4: Biến `WeatherCard` thành consumer**

Bỏ mọi `fetchWeather` / `useEffect` fetch trong `weather-card.tsx`; đọc `const { weather, setWeatherPlace } = useAppState();`. Giữ nguyên toàn bộ UI, trạng thái loading/error, ô tìm thành phố và nút định vị — chỉ đổi *nguồn* dữ liệu.

- [ ] **Step 5: Chạy test, xác nhận xanh**

```bash
npx vitest run src/components/dashboard/weather-card.test.tsx && npx tsc --noEmit
```

Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/components/app/state-provider.tsx src/components/dashboard/weather-card.tsx src/components/dashboard/weather-card.test.tsx
git commit -m "feat(u2a): weather has one source, so the hero and the card never disagree"
```

---

### Task 6: Hero bầu trời

**Files:**
- Modify: `src/components/dashboard/hero-banner.tsx` (viết lại), `src/components/app/today-page.tsx` (truyền prop mới)
- Create: `src/components/dashboard/hero-banner.test.tsx`

**Interfaces:**
- Consumes: `SKY_STYLES`, `SKY_GREETINGS`, `skyPhaseAt` (Task 2); `ProgressRing` (Task 3); `app.weather` (Task 5).
- Produces: `HeroBanner({ bubble, celebrate, hour, viewModel, weather })` — `hour` tiêm được để test đặt mặt trời ở đâu tuỳ ý.

Nội dung theo spec §4.1: dòng ngày + thời tiết gộp · lời chào theo buổi + câu của Nếp · 🔥 chuỗi lớn + "kỷ lục N ✦" + 7 chấm T2→CN + vòng tiến độ hôm nay. Mobile: co lại còn chào + chip 🔥 + vòng tiến độ, 7 chấm xuống dưới.

- [ ] **Step 1: Viết test đỏ — `src/components/dashboard/hero-banner.test.tsx`**

```tsx
import { render, screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";

import { HeroBanner } from "@/components/dashboard/hero-banner";
import {
  buildDashboardViewModel,
  createInitialDashboardState,
  getDashboardToday
} from "@/components/dashboard/dashboard-data";

function viewModel() {
  const day = getDashboardToday();

  return buildDashboardViewModel(createInitialDashboardState(day), day);
}

const NO_WEATHER = { status: "loading" as const, snapshot: null, place: { name: "Sài Gòn", latitude: 10.8, longitude: 106.7 } };

describe("HeroBanner", () => {
  it("greets by the hour it is handed, not by the machine's clock", () => {
    render(<HeroBanner celebrate={false} hour={8} viewModel={viewModel()} weather={NO_WEATHER} />);
    expect(screen.getByText(/Chào buổi sáng/)).toBeTruthy();
  });

  it("wears the evening sky after dark", () => {
    const { container } = render(
      <HeroBanner celebrate={false} hour={21} viewModel={viewModel()} weather={NO_WEATHER} />
    );

    expect(screen.getByText(/Chào buổi tối/)).toBeTruthy();
    expect(container.querySelector(".from-sky-evening-from")).toBeTruthy();
  });

  it("shows today's progress as a ring a screen reader can read", () => {
    const model = viewModel();

    render(<HeroBanner celebrate={false} hour={8} viewModel={model} weather={NO_WEATHER} />);

    const ring = screen.getByRole("progressbar", { name: /Tiến độ hôm nay/ });

    expect(ring.getAttribute("aria-valuenow")).toBe(String(model.today.completedHabits));
    expect(ring.getAttribute("aria-valuemax")).toBe(String(model.today.totalHabits));
  });

  it("draws the seven-day chain with a label per day", () => {
    render(<HeroBanner celebrate={false} hour={8} viewModel={viewModel()} weather={NO_WEATHER} />);

    expect(screen.getAllByRole("listitem")).toHaveLength(7);
  });

  it("says nothing about the weather until it has some", () => {
    // A hero that renders "· undefined°" while loading looks broken.
    render(<HeroBanner celebrate={false} hour={8} viewModel={viewModel()} weather={NO_WEATHER} />);

    expect(screen.queryByText(/undefined/)).toBeNull();
    expect(screen.queryByText(/NaN/)).toBeNull();
  });

  it("folds the weather into the date line once it arrives", () => {
    const weather = {
      status: "ready" as const,
      place: { name: "Sài Gòn", latitude: 10.8, longitude: 106.7 },
      snapshot: {
        temperature: "31°",
        description: "mưa lất phất",
        emoji: "🌦",
        humidity: "94%",
        wind: "8 km/h",
        rainChance: "100%",
        uvIndex: "Vừa",
        feelsLike: "35°",
        isDay: true
      }
    };

    render(<HeroBanner celebrate={false} hour={8} viewModel={viewModel()} weather={weather} />);

    expect(screen.getByText(/31°/)).toBeTruthy();
    expect(screen.getByText(/mưa lất phất/)).toBeTruthy();
  });

  it("shows Nếp's line when there is one, and the daily motivation when there is not", () => {
    const model = viewModel();

    const { rerender } = render(
      <HeroBanner bubble="Hôm nay mình đi từ từ nhé 🌿" celebrate={false} hour={8} viewModel={model} weather={NO_WEATHER} />
    );
    expect(screen.getByText("Hôm nay mình đi từ từ nhé 🌿")).toBeTruthy();

    rerender(<HeroBanner celebrate={false} hour={8} viewModel={model} weather={NO_WEATHER} />);
    expect(screen.getByText(model.motivation)).toBeTruthy();
  });
});
```

> `WeatherSnapshot` có field gì thì dựng đúng field đó — đọc `src/components/dashboard/weather-data.ts` trước, `tsc` sẽ bắt nếu sai.

- [ ] **Step 2: Chạy test, xác nhận đỏ**

```bash
npx vitest run src/components/dashboard/hero-banner.test.tsx
```

Expected: FAIL — `HeroBanner` chưa nhận `hour`/`weather`, chưa có lời chào theo buổi.

- [ ] **Step 3: Viết lại `src/components/dashboard/hero-banner.tsx`**

Yêu cầu bắt buộc của bản viết lại:

1. Bỏ **sạch** token v2 (`matcha`, `sakura`, `plum`, `mauve`, `wafer`, `butter`, `honey`, `soft-panel`). Chỉ dùng token U0 + token bầu trời.
2. `const phase = skyPhaseAt(hour ?? new Date().getHours());` — `hour` là prop optional, mặc định đồng hồ thật.
3. Panel: `cn("relative overflow-hidden rounded-lg p-5 sm:p-6", SKY_STYLES[phase].panel, SKY_STYLES[phase].ink)`.
4. Dòng ngày: `viewModel.date.longLabel`, và **chỉ khi** `weather.status === "ready" && weather.snapshot` mới nối thêm `· {emoji} {temperature} {description}`.
5. `<h1>`: `{SKY_GREETINGS[phase]}, Sếp ơi`.
6. Câu của Nếp: `bubble ?? viewModel.motivation` (voice pack đã qua guard no-guilt).
7. Bên phải: 🔥 `viewModel.streak.current` cỡ lớn · `kỷ lục {viewModel.streak.best} ✦` · `<ProgressRing label={`Tiến độ hôm nay ${done}/${total}`} size="lg" target={total} value={done}>` hiển thị `{done}/{total}` ở giữa.
8. 7 chấm là `<ul>` với 7 `<li>`, mỗi `<li>` có `aria-label` dạng `Ngày {label}: {STATUS_LABELS[status]}`. Chấm hôm nay viền `--action`.
9. Mobile: 7 chấm ở block riêng `sm:hidden` bên dưới; khối phải `hidden sm:flex`.
10. Giữ `<CelebrationOverlay show={celebrate} />` ở cuối.
11. Trang trí: **tối đa 2** chi tiết mờ, `aria-hidden`, `pointer-events-none`.

- [ ] **Step 4: Truyền prop mới trong `today-page.tsx`**

```tsx
        <HeroBanner
          bubble={app.bubble}
          celebrate={app.celebrate}
          viewModel={app.viewModel}
          weather={app.weather}
        />
```

- [ ] **Step 5: Chạy test, xác nhận xanh**

```bash
npx vitest run src/components/dashboard/hero-banner.test.tsx
```

Expected: PASS.

- [ ] **Step 6: Bốn cổng**

```bash
npx tsc --noEmit && npx eslint . --max-warnings=0 && npx vitest run && npx next build
```

Expected: cả 4 xanh.

- [ ] **Step 7: Kiểm chứng bằng trình duyệt thật**

Mở `/dashboard`, rồi trong console đổi giờ để xem cả ba bầu trời — **và đọc màu chữ tính toán được**, vì đây đúng là loại lỗi mà U0 đã bị `.font-display` cho ăn quả lừa (test xanh, mắt thấy sai):

```js
getComputedStyle(document.querySelector("section")).backgroundImage
```

Kiểm: (a) ba bầu trời khác nhau thật · (b) buổi tối chữ sáng, đọc được · (c) vòng tiến độ khớp số habit đã xong · (d) 7 chấm đúng 7 · (e) mobile 375px không tràn ngang.

- [ ] **Step 8: Commit**

```bash
git add src/components/dashboard/hero-banner.tsx src/components/dashboard/hero-banner.test.tsx src/components/app/today-page.tsx
git commit -m "feat(u2a): the hero is a sky now, and it changes with the day"
```

---

### Task 7: Tài liệu

**Files:**
- Modify: `AGENTS.md`, `HANDOFF.md`

- [ ] **Step 1: `AGENTS.md`** — thêm vào khối quy ước:

```markdown
- **Bầu trời là ba BỘ token, không phải ba class** (`sky.ts` + `--sky-{phase}-{from,to,ink,ink-soft}`).
  Buổi tối là nền tối nên ink lật sang sáng — vì thế ink thuộc về từng buổi chứ không dùng chung
  `--ink`. `design-tokens.test.ts` kiểm ink trên **cả hai** đầu gradient; kiểm một đầu là tự lừa mình.
- **Class Tailwind phải xuất hiện nguyên văn trong source.** `SKY_STYLES` viết đủ chuỗi thay vì
  ghép template — class Tailwind không "nhìn thấy" được là class nó không sinh ra.
- **Vòng tiến độ chỉ có một**: `src/components/ui/progress-ring.tsx`. Đừng viết lại conic-gradient.
- **Thời tiết thuộc `StateProvider`** — một fetch cho cả app. Hero và thẻ thời tiết đọc cùng
  `app.weather`, nên không bao giờ hiện hai con số khác nhau.
```

Cập nhật dòng `Current state: N tests green` theo số thật.

- [ ] **Step 2: `HANDOFF.md`** — thêm dòng U2a vào bảng đã ship, cập nhật nhánh + số test, ghi rõ phần còn lại của U2 (**U2b** tab Ngày/Tuần + lưới tuần · **U2c** widget thành chip + sân sau).

- [ ] **Step 3: Bốn cổng lần cuối + commit**

```bash
npx tsc --noEmit && npx eslint . --max-warnings=0 && npx vitest run && npx next build
git add AGENTS.md HANDOFF.md
git commit -m "docs: U2a shipped — the sky hero and two primitives"
```

- [ ] **Step 4: Hoàn tất nhánh**

**REQUIRED SUB-SKILL:** `superpowers:finishing-a-development-branch`.

---

## Self-Review

**1. Spec coverage (§4.1).** Gradient theo buổi → Task 1+2. Trang trí ≤2 chi tiết mờ → Task 6 điểm 11. Dòng ngày + thời tiết gộp → Task 5 + Task 6 điểm 4. Lời chào theo buổi → Task 6 điểm 5. Câu của Nếp qua voice pack → Task 6 điểm 6. 🔥 chuỗi + kỷ lục + 7 chấm + vòng tiến độ → Task 6 điểm 7–8. Mobile co lại → Task 6 điểm 9. Hai primitive nợ từ U0 → Task 3, 4.

**Gap có chủ ý, ghi rõ:** 7 chấm hiện dùng `viewModel.streak.chain` (7 ngày gần nhất) chứ **chưa** phải T2→CN như spec viết, và **chưa** có trạng thái 🍃 nghỉ chủ đích — 🍃 lá chắn thuộc U3. Lưới T2→CN đúng nghĩa nằm ở U2b cùng week grid. Phải nói thẳng điều này trong PR.

**2. Placeholder scan.** Task 5 Step 1 là bước *đọc* có chủ đích (không phải placeholder): shape của `saveWidgetSettings`/`WeatherSnapshot` phải đọc từ source chứ không đoán, và plan nói rõ như vậy ở Task 5 Step 3 + Task 6 Step 1.

**3. Type consistency.** `SkyPhase` (Task 2) dùng ở Task 6. `ProgressRing({ label, target, value, size, tone, children })` (Task 3) khớp call site ở Task 3 Step 5 và Task 6 điểm 7. `app.weather` (Task 5) khớp prop `weather` của `HeroBanner` (Task 6) và fixture `NO_WEATHER` trong test. `TabSwitch` (Task 4) chưa có consumer trong U2a — cố ý, U2b nối vào.

**4. Rủi ro.** Khai báo `sky` trong Tailwind **ghi đè palette `sky` mặc định**. Task 1 Step 4 bắt kiểm `rg "sky-[0-9]" src` trước; nếu có chỗ dùng thì đổi khoá thành `skyband`.
