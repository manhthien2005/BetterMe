import type { ThemeDefinition, ThemeId } from "@/types";

// TODO: Register four complete theme definitions during T-009.
export function getTheme(_id: ThemeId): ThemeDefinition {
  throw new Error("not implemented");
}
