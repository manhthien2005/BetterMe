import type { ISODateString } from "@/types";

export interface CalendarViewProps {
  selectedDate: ISODateString;
}

// TODO: Render the accessible month grid during T-015.
export function CalendarView(_props: CalendarViewProps) {
  return null;
}
